# AI Firehose: Roadmap and Backlog

The prioritized backlog of what to build next. This is the fuel for continuous, autonomous development. A build session (in-turn parallel subagents, or a `/schedule` cron) reads this file plus `CLAUDE.md` and `LESSONS_LEARNED.md`, pulls the top unblocked item, ships it end to end, verifies against oracles (`npm run check:generated`, `npm test`, and `npm run build` clean), commits with a verbose message, then checks the item off here and adds anything it discovered. Run to completion: keep going until the unblocked list is empty or a hard blocker appears, and do not stop to ask between items.

Items that need Eric (credentials, deploys, pushes) are quarantined at the bottom and must not be done autonomously.

## Bigger Bets (Eric's Direction, 2026-06-03)

North star refresh: this site is Eric's life's work and AI-engineering portfolio piece, built so he can face the daily AI firehose in a structured way and see real trends instead of relying on anecdotal memory. It must be elegant as well as useful, held to a bar a discerning hiring engineer would respect. Go 100 percent. Sibling projects to inherit from: spokenhistory.org (`C:\civil`, the most advanced Pinecone/Voyage and agent harness Eric has built, and the one whose harness kept evolving after the others), worldthought.com, and aigamma.com.

- [ ] Inherit the best of the `C:\civil` (spokenhistory.org) harness. Recon done (civil now lives at `C:\OLD_civil` and `D:\civil`; it shares a common-ancestor harness with this repo, so only the deltas matter). Prioritized ports:
  - RAG substrate (worker): (a) rerank the real stored text, not the truncated summary, by storing a `text` metadata field at upsert and reranking it (`worker/lib/retrieve.mjs`, the `worker/pipeline/run.mjs` upsert); (b) a golden-set retrieval eval harness as a manual gate (port civil's rag/eval/run_eval.mjs plus a roughly 10-row AI golden set asserting on concepts, kind, and a score floor); (c) a Pinecone `/vectors/update` metadata-only backfill (no re-embed); (d) dedupe-by-source with over-fetch; (e) query-by-vector-id for "more like this"; (f) Voyage embed retry and backoff (civil rag/embed.mjs); (g) real chunking for the transcript path (bigger, do after the eval harness exists so it can prove it helps).
  - Harness (durable docs): (a) a regenerate-and-diff CI gate, a gate class we still lack: rebuild the derived glossary artifacts and fail on any diff, catching a content edit with no rebuild; (b) a fresh-eyes resume doc with a "Things NOT to do" negative-knowledge list and on-resume health-check commands; (c) a longitudinal session log with an atomic per-phase commit rule and a concurrent-agent coordination note; (d) a pre-flight credential and environment gate before any paid worker run; (e) tell the non-Claude vendor pointers to read past the Claude-only pacing section.
  - Data-sourcing hardening: a contact-bearing User-Agent, internal polite pacing with abort-on-403-or-429 for institutional hosts, resumable checkpoints, a greater-than-50-percent orphan-prune abort guard, and scoping orphan deletion to the kinds the current run produced so a new source never orphans another source's vectors. Civil uses no Apify; it is single-source deep enrichment.
- [ ] Touch as many data points as possible. Add and diversify sources and signals, and keep actively proposing other data points worth ingesting (model releases, benchmarks and leaderboards, funding and M&A, policy and regulation, conference papers, release velocity of key AI libraries, high-signal social). Eric will pay for Apify (about 50 USD per month) for professional data mining where a from-scratch adapter is blocked (for example X/Twitter, or sources that 403 datacenter IPs). Keep authority weights low for noisy sources so they enrich the corpus without distorting the trend ranking. Prioritized additions from recon, free and reuses-existing first: the Hugging Face model hub trending feed (`/api/models?sort=trending`, the single biggest gap, weight about 0.78), PyPI and npm release velocity for core AI libraries (snapshot-diff, about 0.65), uncovered expert newsletters via RSS into `sources/blogs.json` (Import AI, The Batch, Ahead of AI, Interconnects, Simon Willison, about 0.72), the Federal Register policy API (about 0.8), and the lab blogs that do have RSS. Then: benchmark and leaderboard movements (LMArena, the Open LLM Leaderboard, SWE-bench, snapshot-diff, about 0.85), AI podcasts via the existing Whisper path (about 0.75), Papers With Code (about 0.72), and GitHub star-velocity (about 0.65). Spend the Apify budget on the two cases that genuinely need it: the no-RSS lab blogs (OpenAI, xAI) and X/Twitter. Every velocity or leaderboard source should snapshot-diff against `worker/.cache` and emit only changes, reusing the content-hash gate so a board reflects movement, not restatement.
- [ ] Build a creative, non-idempotent demonstration of how an agentic harness works, using THIS project as the living example. An agentic harness is the scaffolding that lets an agent or a fleet operate reliably and autonomously: conventions it auto-loads (`CLAUDE.md`, `AGENTS.md`), persistent memory that does not go stale (the durable-docs protocol, `LESSONS_LEARNED.md`, the run log), automated verification that the docs and knowledge graph and writing rules cannot rot (the gates), and a multi-agent fan-out against idempotent, deterministic contracts. The demonstration should pull LIVE state (glossary size, lessons-session count, gate count, source count, last ingest) so it changes as the project evolves, not a static diagram. Useful to a reader, and a portfolio artifact that shows Eric built one.
- [ ] Elegance pass: hold every surface to portfolio quality (typography, motion, color, copy, accessibility), not just functional correctness.

## P0: Cost-Flat Daily RAG Refresh Before Worker Deploy

- [x] Add a committed vector manifest and hash-gated embedding plan before enabling the scheduled worker. `worker/pipeline/vector_manifest.mjs` records vector text and metadata hashes in `worker/.cache/vector_manifest.json`; `run.mjs` and `embed_glossary.mjs` embed only new or changed text, update metadata-only drift without Voyage, and log embedded, metadata-only, unchanged, and deleted counts.
- [x] Replace full Pinecone ID listing as the normal prune path. The audit hit Pinecone's monthly read-unit limit while listing ids, so routine stale deletion now diffs the old manifest against retained corpus and durable glossary records. `listIds` remains only for manual reconciliation or bounded drift probes.
- [ ] Add a one-time RAG reconciliation playbook before the first Fly deploy. Local artifacts show 258 retained corpus items and 600 durable glossary entries, while Pinecone stats showed 639 total vectors, so the index is probably not fully synchronized with the served corpus. Once read/write budget is available, verify actual prefix counts, backfill missing corpus vectors once, seed the manifest, then make daily runs incremental.
- [x] Make the retrieve path rerank the stored text, not only `summary`. Corpus and glossary vectors now store trimmed `metadata.text`, `worker/lib/retrieve.mjs` prefers it for Voyage rerank, and `summary` remains the display field.
- [x] Add an offline "regenerate and diff" gate for generated artifacts. `scripts/check_generated_fresh.mjs` runs the glossary, harness, and creators generators, then fails on diffs in their served artifacts; CI runs it via `npm run check:generated` before tests.

## P1: Unblocked Work (Ship Autonomously)

- [ ] Trim served-artifact payloads (the "ship only what a page renders" rule). DONE this session: the attention boards now ship only id, label, attention, delta, sparkline, and a two-flag outlier (`worker/pipeline/boards.mjs`), reshaped via `worker/pipeline/recompute_boards.mjs`, 49 percent smaller with no value drift. NOT viable: the digest `movers` and `outliers` are read by the MCP `whats_new` tool and the `integrity.test` referential check, so they are not dead weight (a reminder to audit every consumer, not just `src/`). REMAINING: split a slim wiki-link matcher index from the heavy glossary `index.json` so `def_snippet`, the largest single payload, does not load on every route.
- [x] Scope the `riseIn` entrance animation to the Home page only (a `home-stack` marker class in `src/styles/theme.css` and `src/pages/Home.jsx`), so it no longer re-fires on every route navigation. Reduced-motion handling is universal, so it still applies.
- [ ] Expand worker test coverage. DONE this session: `computeBoards` (the shared pure board builder, `worker/pipeline/boards.test.mjs`: the kind x horizon grid, served entity shape, the attention filter, the sort, the 16-cap, horizon windowing, determinism, empty-safety) and `briefing` state-hash idempotency (`worker/pipeline/briefing.test.mjs`: stable hash for identical state, moves on a salient change, ignores cosmetic relabels so Opus is not re-run needlessly). REMAINING: `transcript` caption parsing beyond the current `vttToText` case; each source adapter's normalized item shape (needs the pure normalizer separated from the network `fetch`, a small refactor, before it can be unit-tested without mocking the network); and `boards` versus `recompute_boards` parity (blocked on the board-window anchor unification in P1.5, since the two anchor differently today).
- [x] Harden `check_generated_fresh` against `creators.json` RSS flakiness. DONE (Session 25): chose the corpus-only-mode option. `build_creators.mjs` is now corpus-only and deterministic by default (live RSS demoted to an explicit `{ live: true }` / `--live` opt-in that the build, gate, and worker do not use), with the `generated` stamp and the retention cutoff anchored to the newest corpus item and the caller-dependent `source` field removed, so the gate is byte-identical on regeneration and on any future date (proven by running the gate under a clock set five months ahead). The same wall-clock wart in `build_harness.mjs` was fixed in the same pass (it now reuses its committed `generated`, like `build_glossary.mjs`), and `prebuild.mjs` promotes creators from a soft to a hard gate. See `LESSONS_LEARNED.md` Session 25.
- [ ] Regenerate `mcp/package-lock.json` to drop the `ai-firehose` file self-dependency that creates the node_modules symlink loop: from the `mcp/` directory, remove `node_modules` and the lockfile, then reinstall. GOTCHA (Session 24): run `npm install` from WITHIN `mcp/` (`cd mcp && rm -rf node_modules package-lock.json && npm install`), NOT `npm install --prefix mcp` from the repo root, which re-adds `ai-firehose: file:..` to `mcp/package.json` (an npm `--prefix`-from-parent footgun) and makes it worse. Low priority: the self-dep is a dev-only lockfile wart, since the published package ships the clean `package.json` (plus `index.mjs` and `README.md`), not the lockfile.
- [ ] Delete or harden the vestigial `worker/pipeline/enrich_hub_first_seen.mjs` (found in the Session 24 bug-scan). It is a one-time `first_seen` backfill that is now SUPERSEDED: `first_seen` is computed natively in `worker/pipeline/run.mjs` (the network stage) and `scripts/build_glossary.mjs`, and the script is referenced nowhere (confirmed by a repo-wide grep). Its header comment claims it is idempotent, but it rebuilds each hub from a HARDCODED key list that omits the durable fields (`body`, `figure`, `related`, `category`, `durable`, `def_snippet`), so re-running it today would strip authored content and figures from the durable hubs. The generated-fresh gate would catch the diff, but it is a latent footgun for a future agent who trusts the "Run:" comment. Disposition (Eric's call): delete the file, or change its hub rewrite to `{ ...h, first_seen: firstSeen[h.id] || h.first_seen || null }` so it actually preserves fields. Left unmodified by the overnight loop because it is dead code and a delete-vs-fix judgment call, not a bug in the live pipeline.
- [ ] Deepen and illustrate the durable glossary entries: more QA passes on the authored Markdown under `content/glossary/`, and expand the cited-image sidecar `content/glossary/images.json`. Parallelizable, one agent per category.

## P2: Unblocked, Judgment Calls

- [x] Add doc-accuracy gates for model tiers and counts, made tolerant of phrasing so they do not false-positive on "over 450". `scripts/check_doc_accuracy.mjs` now asserts the configured model tiers are reflected in `CLAUDE.md` and that public count claims match the served glossary index, atlas, and checker inventory.
- [ ] Make the AI-grown resolver dedupe against the DURABLE layer. `worker/pipeline/concepts.mjs` resolves a discovered concept only against the corpus taxonomy, so surface variants of an authored concept spawn thin parallel hubs and fragment the boards (fixed on the snapshot by `scripts/fold_corpus_concepts.mjs`, but the worker will re-fragment on its next run). Embed the durable surface forms into the resolver's candidate set, or run the fold (`scripts/fold_corpus_concepts.mjs`) as a pipeline step after the glossary build in `worker/pipeline/run.mjs`. The genuinely-new discovered concepts (about 360 in the current snapshot) are then the authoring backlog: real durable entries, written linearly main-thread at full Opus strength.
- [ ] SEO: add a real 1200 by 630 social card image and reference it as `og:image` and `twitter:image`, then raise `twitter:card` to a large summary. The canonical link and the theme-color sync already shipped.
- [ ] Build an X (Twitter) source adapter following the uniform shape of the existing `worker/sources/` adapters (a stretch goal noted in `CLAUDE.md`). Respect rate limits and give it a low `source_authority_weight` so it enriches without distorting the trend ranking.
- [ ] Save / bookmark items and concepts for later (the deferred returning-visitor feature, high value and clean): a star control on `ItemCard` (key on `url`) and concept rows, persisted to `aifh:saved:v1`, plus a "Saved" view. Store a snapshot of each saved item so retention pruning does not orphan the bookmark. Pure logic in a new saved lib plus a hook, mirroring the seen and srs split. Was scoped out of Session 11 only to keep the ItemCard read-state integration focused.
- [ ] First-time vs returning onboarding: a one-time, dismissible intro gated on an `aifh:onboarded` flag that teaches the personalization features (Review, Save) which create the return triggers. Keep it quiet and editorial per the Session 8 voice rule.
- [ ] Search (the command palette) is currently removed from the header: its dropdown used a translucent panel that bled the page content behind it into an unreadable collision (see LESSONS_LEARNED Session 17). The `src/components/CommandPalette.jsx` file remains, unmounted. Before remounting it, give the panel an opaque `var(--surface)` fill (as `.app-menu-panel` now has) and add the Tab focus trap it still lacks (the dialog is `role="dialog" aria-modal="true"` but Tab can leave the panel; trap it or set the page `inert` while open).

## P1.5: Board-Data Quality (verify against a live worker run)

- [ ] Unify the board window anchor so the docs' single-source claim is literal: `run.mjs` anchors board windows to `Date.now()` while `recompute_boards.mjs` anchors to the newest item timestamp, so a replay on a later day re-anchors day/week membership. Make `run.mjs` pass the same data-anchored `todayMs` to `buildSeries`/`computeBoards` (keeping wall-clock `Date.now()` for prune, `ingested_at`, and the RSS `lastBuildDate`), then add the `boards` vs `recompute_boards` parity test already listed above. This changes day/week board output, so verify it on a real worker run before trusting it (Session 11 corrected the doc rather than guess the behavior change).
- [ ] Thin-window guard for the day and week boards: today all 8 "What Moved" movers on those two shortest, most-viewed horizons tie at `attention: 1` and `ratio/momentum: 145`, so the front-page board shows 8 indistinguishable rows. Require `attention >= 2` to rank, or de-tie by a secondary key, or widen the day/week window. (`month`/`quarter` are healthy.)
- [ ] Recompute or suppress all-zero sparklines: several quarter-board entries carry real `attention` and `delta` but an all-zero `sparkline`, so a notable entry renders a flat trend line. Recompute the sparkline from the same windowed counts that feed `attention` in `worker/pipeline/boards.mjs`, or suppress a flat line for a high-attention entry.

## Needs Eric (Do Not Do Autonomously)

- [ ] Deploy the Fly worker. This turns on the daily data refresh and is the single most important pending item. Needs a Fly account and secrets.
- [ ] Push `main` (which auto-deploys via Netlify) and publish or republish the `mcp` package to npm. Pushes are Eric's call under the push-sparingly rule.

## Recently Shipped

- Creators directory and one-command educator onboarding: a browse-and-subscribe roster on the Watch page driven by the ingestion registry (`scripts/build_directory.mjs` and its pure core `scripts/lib/directory.mjs` produce `public/data/directory.json`, rendered by `src/components/CreatorDirectory.jsx`), corpus-only and deterministic so it is a hard prebuild and generated-fresh gate; `scripts/onboard_youtube.mjs` to drop a list of handles and have them resolved, added to the registry, and listed in the directory in one command; and a three-month retention cap on the featured-video stream in parity with the corpus prune (`scripts/build_creators.mjs`, keyed on `published_at`). The Watch page consolidates the directory over the latest-videos funnel. Runbook rewritten as `docs/ONBOARD_YOUTUBE_CHANNEL.md`. All gates green.
- Cost-flat RAG refresh hardening: committed vector manifest, hash-gated corpus and durable-glossary embedding, manifest-driven deletion instead of routine Pinecone listing, stored-text rerank, and a generated-artifact regenerate-and-diff CI gate.
- Deep health check plus the returning-visitor layer (Session 11): a client-side retention layer that makes the firehose conquerable (a "new since your last visit" signal, per-item read state with a "you have cleared this window's wire" completion banner, learning-path progress, and dateline nudges, all `localStorage`); a real SM-2-lite spaced-repetition Review (`src/lib/srs.js` pure and unit-tested, `src/hooks/useSrs.js`) replacing the stateless shuffle; the TrendBoard breakout flag reworked from a title-crowding pill into a shaded color-coded row; worker fetch timeouts on every pipeline HTTP call; real error states (shared `LoadError.jsx`) for Glossary, KindView, and TechniqueHub; a `javascript:`-scheme link sanitizer in `richtext.js`; dead-code removal (`fetchByIds`, `enrich_hub_rotation.mjs`); the embed/upsert length assert and atomic cache write; and the corrected board-anchor doc claim. All gates green, 87 tests, build clean, verified by headless screenshot.
- Doc-drift and small-defect pass (commit 50aa0c0): the Python overstatement, the Sonnet versus Opus briefing comment, the stale 356 count, the removed `trail` field, the MCP `define` related-mesh bug, and the Explore spectrum-dot accessible name.
- Robustness and continuity batch: the `useData` empty-versus-error fix for the Netlify SPA fallback, theme-color that tracks the active theme, a canonical link, the Pinecone readiness guard, the Hugging Face id-less skip, the definitions-cache prune, the `publish.sh` required-env guard, the tree-to-doc anti-staleness gate, the `mcp/` subsystem documented, and this ROADMAP.
- Payload trim: the served attention boards now ship only the fields TrendBoard renders (`worker/pipeline/boards.mjs` plus the 12 reshaped board files), 49 percent smaller, with no value drift.
- The `riseIn` front-page entrance animation is scoped to Home (`home-stack`), so it no longer replays on every route change.
- Glossary widening: 98 new Opus-authored durable concepts across all 31 categories (453 to 551 authored entries, 941 total index entries with the corpus), each woven into the existing cross-link mesh, via a 10-agent parallel fan-out. All gates green.
