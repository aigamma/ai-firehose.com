{
  "id": "residual-connection",
  "label": "Residual Connection",
  "kind": "technique",
  "category": "Deep Learning Architectures",
  "aliases": [
    "skip connection",
    "residual",
    "shortcut connection",
    "ResNet"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A shortcut that adds a layer's input directly to its output, so the layer need only learn a residual correction, which lets gradients flow unobstructed and makes very deep networks trainable.",
  "body": [
    {
      "type": "p",
      "text": "A residual connection, also called a skip connection, routes the input of a layer or block directly to its output by addition, so the block computes its transformation plus an identity copy of what came in. Formally, where a plain block would output some function of its input, a residual block outputs that function added to the input itself. The layer is therefore only responsible for learning the residual, the difference between the desired output and the input, which is often easier than learning the full mapping from scratch."
    },
    {
      "type": "p",
      "text": "Residual connections matter because they made genuinely deep networks trainable for the first time. Before they were introduced in the ResNet architecture in 2015, stacking more layers past a point made networks harder to optimize and actually raised training error, a degradation problem distinct from overfitting. The skip path turned this around and enabled networks of hundreds or even a thousand layers. The improvement came not from added capacity but from a far better optimization landscape."
    },
    {
      "type": "p",
      "text": "The mechanism that explains this is gradient flow. During backpropagation, the addition in a residual block sends the gradient backward along two routes, one through the block and one straight through the identity shortcut. That identity path carries the gradient unattenuated to earlier layers, sidestepping the repeated shrinking that causes the vanishing gradient in deep stacks. A useful intuition is that a deep residual network behaves like an ensemble of many shorter paths of varying depth, so it degrades gracefully and trains stably."
    },
    {
      "type": "p",
      "text": "The residual connection is now a near-universal building block. It is paired with batch normalization in convolutional networks and with layer normalization in transformers, where every attention mechanism sublayer and feedforward sublayer is wrapped in a residual connection. Few ideas have been as quietly load-bearing: without it, the very deep convolutional neural network and the modern transformer would both be far harder to train."
    }
  ],
  "related": [
    {
      "slug": "vanishing-gradient",
      "label": "Vanishing Gradient",
      "kind": "technique"
    },
    {
      "slug": "batch-normalization",
      "label": "Batch Normalization",
      "kind": "technique"
    },
    {
      "slug": "layer-normalization",
      "label": "Layer Normalization",
      "kind": "technique"
    },
    {
      "slug": "convolutional-neural-network",
      "label": "Convolutional Neural Network",
      "kind": "technique"
    },
    {
      "slug": "backpropagation",
      "label": "Backpropagation",
      "kind": "technique"
    },
    {
      "slug": "attention-mechanism",
      "label": "Attention Mechanism",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/residual-connection.svg",
    "alt": "Two network blocks side by side: a regular block on the left whose stacked layers must learn the full mapping f(x), and a residual block on the right where a skip arrow adds the input x back to the block output, so the layers learn g(x) and the block computes f(x) equals g(x) plus x.",
    "caption": "In a residual block the input is added back to the output through a skip connection, so the stacked layers only need to learn the residual correction g(x) rather than the full mapping. The identity shortcut also gives gradients a clean path backward, which is what makes very deep networks trainable.",
    "credit": "Zhang, Aston and Lipton, Zachary C. and Li, Mu and Smola, Alexander J.",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://d2l.ai/chapter_convolutional-modern/resnet.html",
    "provider": "Dive into Deep Learning"
  }
}
