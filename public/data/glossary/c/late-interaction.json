{
  "id": "late-interaction",
  "label": "Late Interaction",
  "kind": "technique",
  "category": "RAG, Embeddings, and Retrieval",
  "aliases": [
    "ColBERT",
    "late-interaction retrieval",
    "token-level retrieval"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A retrieval method that encodes a query and a document into per-token embeddings and scores relevance by summing each query token's best match against the document tokens (MaxSim), keeping the fine-grained term interactions a single pooled vector discards. It is the middle path, cross-encoder-like precision while staying indexable, bought with the cost of storing a vector per token rather than per document.",
  "body": [
    {
      "type": "p",
      "text": "Late interaction is a middle path between the two classic ways to retrieve text. Ordinary dense retrieval encodes a whole query and a whole document each into one pooled vector and scores them by a single dot product; that is fast and lets a vector database do the search, but compressing a passage into one vector loses detail, so a document that matches the query on one crucial term can be outranked because its average meaning drifts. A cross-encoder fixes the detail problem by feeding query and document together through a model, but it must rerun the model for every query-document pair, so it cannot scale to a large corpus on its own."
    },
    {
      "type": "p",
      "text": "Late interaction, introduced by ColBERT, keeps per-token detail without paying the cross-encoder's price at query time. It encodes the query into a vector per query token and each document into a vector per document token, independently, and relevance is then scored with a late step, after encoding, often called MaxSim: for each query token, find its best-matching document token by similarity, and sum those best matches over the query. Because the document vectors are computed and stored ahead of time, only the query is encoded live, and the scoring is cheap arithmetic over precomputed vectors."
    },
    {
      "type": "p",
      "text": "The name captures the design: the interaction between query and document happens late, at scoring time, rather than early inside a shared encoder. This preserves term-level signal, so late interaction tends to retrieve with accuracy closer to a cross-encoder while staying far more scalable, and it is robust on rare terms and out-of-domain text where single-vector dense retrieval can wash out."
    },
    {
      "type": "p",
      "text": "The trade is storage and engineering. Keeping a vector for every token, rather than one per document, inflates the index by one to two orders of magnitude, so practical systems compress those vectors and use approximate nearest neighbor search to shortlist candidates before the MaxSim scoring. In a retrieval-augmented generation stack it often serves as a strong first-stage retriever or a scalable alternative to a separate reranking pass."
    }
  ],
  "related": [
    {
      "slug": "dense-retrieval",
      "label": "Dense Retrieval",
      "kind": "technique"
    },
    {
      "slug": "reranking",
      "label": "Reranking",
      "kind": "technique"
    },
    {
      "slug": "embedding-model",
      "label": "Embedding Model",
      "kind": "technique"
    },
    {
      "slug": "semantic-search",
      "label": "Semantic Search",
      "kind": "technique"
    },
    {
      "slug": "approximate-nearest-neighbor",
      "label": "Approximate Nearest Neighbor",
      "kind": "technique"
    },
    {
      "slug": "retrieval-augmented-generation",
      "label": "Retrieval-Augmented Generation",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/late-interaction.png",
    "alt": "A diagram of ColBERT. A Query Encoder produces a row of green per-token vectors and a Document Encoder produces a grid of blue per-token vectors (marked offline indexing). For each query token, a MaxSim operator selects its best-matching document token, and the MaxSim values are summed into a single score.",
    "caption": "Late interaction keeps a vector for every token instead of pooling each text to one vector. The document tokens (blue) are encoded and indexed offline; at query time each query token (green) finds its best-matching document token via MaxSim, and those best matches are summed into the relevance score. Doing the interaction late, after encoding, preserves term-level detail while keeping the document side precomputable.",
    "credit": "Omar Khattab, Matei Zaharia (ColBERT)",
    "license": "MIT",
    "license_url": "https://opensource.org/licenses/MIT",
    "source": "https://github.com/stanford-futuredata/ColBERT",
    "provider": "ColBERT (Stanford)"
  }
}
