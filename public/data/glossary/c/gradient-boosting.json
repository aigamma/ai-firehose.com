{
  "id": "gradient-boosting",
  "label": "Gradient Boosting",
  "kind": "technique",
  "category": "Core Machine Learning",
  "aliases": [
    "gradient boosting",
    "GBDT",
    "XGBoost",
    "LightGBM"
  ],
  "durable": true,
  "source": "authored",
  "definition": "An ensemble method that builds a strong predictor by adding simple models (usually shallow decision trees) one at a time, each trained to correct the running sum's errors, the approach behind the libraries that dominate tabular data.",
  "body": [
    {
      "type": "p",
      "text": "Gradient boosting builds a powerful model out of weak ones by adding them sequentially, each new model aimed at the mistakes of the ensemble so far. Concretely, it fits a shallow decision tree to the current errors (more precisely, to the negative gradient of the loss), adds that tree to the running prediction scaled by a small learning rate, recomputes the errors, and repeats. Hundreds of small trees, each nudging the prediction toward the residual the others missed, sum into a highly accurate model."
    },
    {
      "type": "p",
      "text": "The contrast with random forests is the clearest way to understand it. A random forest uses bagging: it trains many trees independently and in parallel on resampled data, then averages them, which mainly reduces variance. Gradient boosting is sequential and dependent: each tree exists to fix the previous ones, which mainly reduces bias. That sequential error-correction is what lets boosting reach very high accuracy."
    },
    {
      "type": "p",
      "text": "On tabular and structured data, gradient boosting is often the method to beat. The optimized implementations, XGBoost, LightGBM, and CatBoost, are the default winners of countless data-science competitions and a mainstay of production models on spreadsheet-like data, where deep learning frequently does not pay off."
    },
    {
      "type": "p",
      "text": "Its power is also a risk: because it keeps fitting residuals, it can overfit if left unchecked. The remedies are a small learning rate, shallow trees, subsampling, and early stopping on a validation set, the knobs that trade a little training accuracy for genuine generalization."
    }
  ],
  "related": [
    {
      "slug": "decision-tree",
      "label": "Decision Tree",
      "kind": "technique"
    },
    {
      "slug": "random-forest",
      "label": "Random Forest",
      "kind": "technique"
    },
    {
      "slug": "ensemble-learning",
      "label": "Ensemble Learning",
      "kind": "technique"
    },
    {
      "slug": "bias-variance-tradeoff",
      "label": "Bias-Variance Tradeoff",
      "kind": "technique"
    },
    {
      "slug": "overfitting",
      "label": "Overfitting",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/gradient-boosting.png",
    "alt": "Line plot of deviance against the number of boosting iterations: the training-set deviance (blue) falls steeply and keeps decreasing toward a low value, while the test-set deviance (red) falls early then flattens and stays above the training curve, the gap widening as iterations grow.",
    "caption": "Gradient boosting builds an additive model one tree at a time, each new tree fitting the errors left by the trees so far, so the loss (deviance) drops as iterations accumulate. The training loss keeps falling while the test loss flattens, the classic signal that adding more stages eventually overfits.",
    "credit": "scikit-learn developers",
    "license": "BSD-3-Clause",
    "license_url": "https://github.com/scikit-learn/scikit-learn/blob/main/COPYING",
    "source": "https://scikit-learn.org/stable/auto_examples/ensemble/plot_gradient_boosting_regression.html",
    "provider": "scikit-learn"
  }
}
