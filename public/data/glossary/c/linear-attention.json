{
  "id": "linear-attention",
  "label": "Linear Attention",
  "kind": "technique",
  "category": "Frontier Architectures",
  "aliases": [
    "linear attention",
    "linear transformer",
    "linearized attention"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A reformulation of self-attention that removes the softmax so the computation reassociates: multiply keys by values first into a small running summary, then each query into that summary, cutting the cost from quadratic to linear and turning attention into a recurrence. Its honest difficulty is quality, a fixed-size state must compress an unbounded history, so it can miss the sharp recall softmax attention keeps.",
  "body": [
    {
      "type": "p",
      "text": "Linear attention makes the attention mechanism scale linearly with sequence length instead of quadratically. Standard self-attention forms a similarity score between every pair of tokens, passes those scores through a softmax, and uses the result to take a weighted average of the values; the all-pairs score matrix is what costs time and memory proportional to the square of the sequence length, and the softmax is what forces that matrix to be built explicitly, because it couples all the scores in a row together through its normalization."
    },
    {
      "type": "p",
      "text": "The key trick is to replace the softmax similarity with a kernel that factorizes. If the similarity between a query and a key can be written as the dot product of two feature maps applied separately to the query and to the key, then the order of operations can change: instead of multiplying queries by keys first to form the big score matrix, the model multiplies keys by values first to form a small fixed-size summary matrix, then multiplies each query into that summary. By associativity the answer is the same, but the large sequence-by-sequence matrix is never materialized, and the cost drops to linear in length with memory that does not grow with the sequence."
    },
    {
      "type": "p",
      "text": "The deeper consequence is that linear attention is mathematically a recurrence. The running summary of keys and values is exactly a hidden state that accumulates as tokens arrive, so a linear attention layer can generate one token at a time in constant memory, just like a state space model. This equivalence is now well understood: linear attention, the deep SSM, and the retentive network are different parameterizations of the same underlying idea, a sequence mixer with a linear recurrence and a fixed-size state, and Mamba's second formulation makes the connection explicit by casting its selective state space layer as a structured form of linear attention."
    },
    {
      "type": "p",
      "text": "Linear attention matters as one of the original and most influential answers to the quadratic cost of transformers, and it remains a live research direction rather than a settled solution. The honest difficulty is simple: quality. A fixed-size state must compress an unboundedly long history, so naive linear attention can underperform softmax attention on tasks that need sharp, precise recall of a specific earlier token, and much of the subsequent work, gating mechanisms, decay terms as in the retentive network, and input-dependent dynamics as in mamba, is aimed squarely at closing that quality gap while preserving the linear cost. It sits at the center of the broader effort to find an efficient successor to all-pairs attention."
    }
  ],
  "related": [
    {
      "slug": "state-space-model",
      "label": "State Space Model",
      "kind": "technique"
    },
    {
      "slug": "retentive-network",
      "label": "Retentive Network",
      "kind": "technique"
    },
    {
      "slug": "mamba",
      "label": "Mamba",
      "kind": "technique"
    },
    {
      "slug": "structured-state-space",
      "label": "Structured State Space",
      "kind": "technique"
    },
    {
      "slug": "hyena",
      "label": "Hyena",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/linear-attention.webp",
    "alt": "Keys and values are compressed into a running summary state that each query reads, instead of comparing against every position; labeled keys, values, summary, and query.",
    "caption": "Linear attention rewrites the attention computation so its cost grows linearly with sequence length instead of quadratically, by maintaining a running summary of past keys and values that each query reads from. It trades some of full attention's expressiveness for the ability to handle very long sequences, and it reveals attention's hidden kinship with recurrent networks.",
    "credit": "Generated by OpenAI gpt-image-1, 2026-06-09",
    "license": "AI-generated",
    "license_url": "https://openai.com/policies/usage-policies/",
    "source": "",
    "provider": "OpenAI"
  }
}
