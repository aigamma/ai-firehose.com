{
  "id": "n-gram-model",
  "label": "N-Gram Model",
  "kind": "technique",
  "category": "NLP Foundations",
  "aliases": [
    "n-gram",
    "language model n-gram",
    "bigram model",
    "trigram model"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A classic statistical language model that estimates the probability of the next word from the previous n minus one words, using counts of short word sequences gathered from a corpus.",
  "body": [
    {
      "type": "p",
      "text": "An n-gram model is a statistical approach to language modeling that estimates the probability of a word given the words immediately before it. An n-gram is a contiguous run of n items, usually words: a bigram is a pair, a trigram is a triple. The model assumes a Markov approximation, that the next word depends only on a short fixed window of preceding words rather than on the entire history, which makes the probabilities tractable to count and store."
    },
    {
      "type": "p",
      "text": "Training an n-gram model is mostly counting. To estimate the chance that \"mat\" follows \"the cat sat on the\", a trigram model looks only at the last two words, \"on the\", and divides the number of times \"on the mat\" appeared by the number of times \"on the\" appeared in the corpus. Because many valid sequences never occur in any finite corpus, raw counts assign zero probability to unseen n-grams, so practical systems apply smoothing, which shifts a little probability mass to unseen events, and backoff, which falls back to shorter n-grams when a longer one is unobserved."
    },
    {
      "type": "p",
      "text": "N-gram models matter because they were the backbone of language technology for decades, driving speech recognition, machine translation, spelling correction, and predictive text keyboards. They are fast, transparent, and need no specialized hardware, and a well-smoothed n-gram model remains a respectable baseline. Their representation also pairs naturally with count-based document methods such as tf-idf, where n-grams capture short phrases that single words miss."
    },
    {
      "type": "p",
      "text": "The defining weakness of the n-gram model is its short, fixed memory. Raising n to capture longer dependencies causes the number of possible sequences to explode and the counts to grow hopelessly sparse, a manifestation of the curse of dimensionality, so in practice n rarely exceeds four or five. The model therefore cannot connect a pronoun to an antecedent ten words back, nor track the subject of a long sentence. It also treats words as discrete symbols with no notion that \"dog\" and \"puppy\" are related, the same gap that word embeddings like word2vec later addressed."
    },
    {
      "type": "p",
      "text": "The n-gram model is the direct conceptual ancestor of modern neural language models. Both assign probabilities to word sequences; the difference is how they represent context. Neural models replaced sparse symbol counts with learned dense vectors and, with the transformer, replaced the fixed Markov window with self-attention that can reach across an entire passage. A large language model is, at bottom, a vastly more capable answer to the same question the n-gram model first posed: what word comes next."
    }
  ],
  "related": [
    {
      "slug": "tf-idf",
      "label": "TF-IDF"
    },
    {
      "slug": "word-embedding",
      "label": "Word Embedding"
    },
    {
      "slug": "word2vec",
      "label": "word2vec"
    },
    {
      "slug": "masked-language-modeling",
      "label": "Masked Language Modeling"
    },
    {
      "slug": "large-language-model",
      "label": "Large Language Model"
    },
    {
      "slug": "self-attention",
      "label": "Self-Attention"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": []
}
