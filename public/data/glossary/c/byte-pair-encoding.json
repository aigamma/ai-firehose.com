{
  "id": "byte-pair-encoding",
  "label": "Byte-Pair Encoding",
  "kind": "technique",
  "category": "Transformers and LLMs",
  "aliases": [
    "BPE",
    "byte pair encoding",
    "byte-level BPE"
  ],
  "durable": true,
  "source": "authored",
  "definition": "The subword algorithm most LLMs use to build their tokenizer, born as a data-compression scheme and adapted to language: it grows a vocabulary by repeatedly merging the most frequent adjacent pair of symbols. Its byte-level variant runs over raw bytes, which guarantees any possible input encodes and removes the need for an unknown token entirely.",
  "body": [
    {
      "type": "p",
      "text": "Byte-pair encoding, usually shortened to BPE, is the algorithm most large language models use to build their tokenizer. It originated as a simple data-compression scheme and was adapted to natural language processing as a way to learn a subword vocabulary from data rather than fixing one by hand, and its appeal is the practical compromise it strikes: frequent words become single tokens, while rare words and never-before-seen strings still decompose cleanly into smaller known pieces."
    },
    {
      "type": "p",
      "text": "Training a BPE vocabulary is an iterative merge procedure. You start by treating each text as a sequence of base symbols, originally individual characters and in the byte-level variant the raw bytes, so nothing is ever out of vocabulary; you then count every adjacent pair of symbols across the corpus, find the single most frequent pair, and merge it into a new combined symbol added to the vocabulary. Repeating this thousands of times grows the vocabulary from base symbols up through common letter groups, then morphemes, then whole frequent words, stopping when it reaches a target size, and the ordered list of merges learned during training is exactly what is replayed at inference time to tokenize new text."
    },
    {
      "type": "p",
      "text": "The byte-level form, used by the GPT family among others, runs BPE over UTF-8 bytes instead of Unicode characters, which guarantees that any possible input, in any language or script, with any emoji or control character, can always be encoded, because every byte value is a valid starting symbol. It removes the need for a special unknown token entirely, at the cost of occasionally spending several tokens on a single unusual character."
    },
    {
      "type": "p",
      "text": "BPE matters because it directly determines the token sequences that flow into a model, and therefore both cost and behavior. A well-fitted BPE vocabulary keeps common text dense, so more meaning fits inside a fixed context window, while a poor fit for a given language inflates token counts and wastes budget. It is the concrete mechanism behind subword tokenization, and understanding it explains why model inputs are counted in tokens, why those token boundaries rarely line up with word boundaries, and why each learned token eventually gets its own row in the token embedding table."
    }
  ],
  "related": [
    {
      "slug": "tokenization",
      "label": "Tokenization",
      "kind": "technique"
    },
    {
      "slug": "token-embedding",
      "label": "Token Embedding",
      "kind": "technique"
    },
    {
      "slug": "context-window",
      "label": "Context Window",
      "kind": "technique"
    },
    {
      "slug": "large-language-model",
      "label": "Large Language Model",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/byte-pair-encoding.webp",
    "alt": "Rows of single-character tiles are progressively merged into larger subword tiles through an arrow, with one newly merged tile highlighted; titled Byte-Pair Encoding.",
    "caption": "Byte-pair encoding builds a subword vocabulary by starting from individual characters and repeatedly merging the most frequent adjacent pair into a single new token. The result lands between characters and whole words: common words become one token while rare ones split into pieces, so the model can spell out anything it has never seen.",
    "credit": "Generated by OpenAI gpt-image-1, 2026-06-09",
    "license": "AI-generated",
    "license_url": "https://openai.com/policies/usage-policies/",
    "source": "",
    "provider": "OpenAI"
  }
}
