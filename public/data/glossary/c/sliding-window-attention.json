{
  "id": "sliding-window-attention",
  "label": "Sliding Window Attention",
  "kind": "technique",
  "category": "Transformers and LLMs",
  "aliases": [
    "local attention",
    "windowed attention",
    "sliding-window attention"
  ],
  "durable": true,
  "source": "authored",
  "definition": "An attention pattern where each token attends only to a fixed window of recent tokens, not the whole sequence, making attention cost linear in length and bounding the KV cache. The clever part is that depth rescues range: because the window applies at every layer, the effective reach grows many windows wide up the stack, exactly as a CNN builds a large receptive field from small filters.",
  "body": [
    {
      "type": "p",
      "text": "Sliding window attention attacks the quadratic cost of full self-attention by limiting how far each token can look. Instead of letting every token attend to all previous tokens, each one attends only to a fixed window of the most recent W tokens, so the work per token becomes constant rather than growing with sequence length, the total attention cost is linear in the sequence, and the kv-cache needed during generation is bounded by the window instead of the full history."
    },
    {
      "type": "p",
      "text": "The obvious worry is that a token can no longer see anything older than its window, but depth rescues range, and that is the keeper. Because the window applies at every layer, information propagates outward as it flows up the stack, much as a convolutional neural network builds a large receptive field from small local filters: a token at layer two can be influenced by a neighbor that, at layer one, already absorbed a token further back, so after several layers the effective reach is many windows wide even though each layer is strictly local."
    },
    {
      "type": "p",
      "text": "This makes sliding window attention a practical way to extend usable context cheaply, and it was a defining feature of models such as Mistral. It is often combined with other mechanisms, for example interleaving a few full-attention layers, or pairing the window with attention sinks that always keep the first tokens, so the model retains a stable anchor."
    },
    {
      "type": "p",
      "text": "The trade is that exact long-range dependencies in a single step are sacrificed for efficiency. When a task genuinely needs precise recall across a very long span, a pure local window can miss it, which is why long-context systems usually blend windowed and global attention rather than relying on either alone."
    }
  ],
  "related": [
    {
      "slug": "self-attention",
      "label": "Self-Attention",
      "kind": "technique"
    },
    {
      "slug": "context-window",
      "label": "Context Window",
      "kind": "technique"
    },
    {
      "slug": "kv-cache",
      "label": "KV Cache",
      "kind": "technique"
    },
    {
      "slug": "flash-attention",
      "label": "Flash Attention",
      "kind": "technique"
    },
    {
      "slug": "large-language-model",
      "label": "Large Language Model",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/sliding-window-attention.png",
    "alt": "A graph of the tokens I am going to sleep now arranged in a ring, labeled sliding connections. Each token is joined by an edge only to its two immediate neighbors in the sequence, so attention links stay local rather than connecting every token to every other token.",
    "caption": "Sliding window attention limits each token to attend only to a fixed window of nearby tokens instead of the whole sequence. This local pattern makes the cost grow linearly with length rather than quadratically. Because the window is applied at every layer, information still propagates across long distances as it flows up the stack.",
    "credit": "Hugging Face",
    "license": "Apache-2.0",
    "license_url": "https://www.apache.org/licenses/LICENSE-2.0",
    "source": "https://huggingface.co/blog/big-bird",
    "provider": "Hugging Face"
  }
}
