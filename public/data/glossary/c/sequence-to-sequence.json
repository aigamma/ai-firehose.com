{
  "id": "sequence-to-sequence",
  "label": "Sequence-to-Sequence",
  "kind": "technique",
  "category": "NLP Foundations",
  "aliases": [
    "seq2seq"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A framework for mapping an input sequence to an output sequence of possibly different length, with an encoder that reads the input into a representation and a decoder that generates the output one step at a time. Its early fixed-vector bottleneck is what attention was invented to fix, and that fix, not the recurrent backbone, turned out to be the idea that mattered.",
  "body": [
    {
      "type": "p",
      "text": "Sequence-to-sequence, often shortened to seq2seq, handles problems where both the input and the output are sequences that need not be the same length. Machine translation is the canonical case, a sentence in one language mapping to a sentence in another, but the same shape fits summarization, question answering, speech transcription, and code generation. Introduced around 2014, the approach made a single neural model able to read a whole sequence and emit a whole sequence, rather than labeling tokens one at a time."
    },
    {
      "type": "p",
      "text": "The architecture has two parts. An encoder consumes the input sequence and compresses it into an internal representation, and a decoder produces the output sequence step by step, emitting one token at a time and feeding each generated token back in to inform the next. In the original formulation both parts were recurrent neural networks, and the encoder squeezed the entire input into a single fixed-length context vector the decoder then unrolled. Training maximizes the probability the model assigns to the correct output sequence, a richer objective than the next-word counts of an n-gram model."
    },
    {
      "type": "p",
      "text": "The early design had a notorious bottleneck, and naming its fix is the keeper: forcing all information about the input through one fixed-length vector lost detail, and quality degraded on long sequences. The remedy was the attention mechanism, introduced in 2015, which lets the decoder look back at all of the encoder's per-token representations and focus on the relevant parts as it generates each output token. Attention removed the single-vector bottleneck and sharply improved translation, and it proved to be the more consequential idea than the recurrent backbone it was bolted onto."
    },
    {
      "type": "p",
      "text": "Sequence-to-sequence is the bridge between the older statistical and recurrent approaches and the transformer era. The encoder-decoder structure it established is exactly the structure the original transformer adopted, except the transformer discarded recurrence entirely and built both halves out of self-attention. Many modern systems are encoder-decoder transformers that descend directly from seq2seq, and the decoder-only large language model is a streamlined relative that keeps the autoregressive generation half."
    },
    {
      "type": "p",
      "text": "The lasting lesson is the generative, conditional view of language: model the output sequence as a probability distribution conditioned on the input, and generate it autoregressively. That framing, plus the attention that rescued it, set the stage for the architectures that followed. Where static embeddings like word2vec gave models a vocabulary, seq2seq gave them a way to read one sequence and write another, the template behind translation, summarization, and conversational generation today."
    }
  ],
  "related": [
    {
      "slug": "self-attention",
      "label": "Self-Attention",
      "kind": "technique"
    },
    {
      "slug": "attention-mechanism",
      "label": "Attention Mechanism",
      "kind": "technique"
    },
    {
      "slug": "n-gram-model",
      "label": "N-Gram Model",
      "kind": "technique"
    },
    {
      "slug": "word-embedding",
      "label": "Word Embedding",
      "kind": "technique"
    },
    {
      "slug": "bert",
      "label": "BERT",
      "kind": "tool"
    },
    {
      "slug": "large-language-model",
      "label": "Large Language Model",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/sequence-to-sequence.svg",
    "alt": "An encoder-decoder diagram for machine translation. A recurrent encoder reads the English tokens They, are, watching, period, and an end-of-sequence marker into a final state. That state initializes a recurrent decoder, which is fed a beginning-of-sequence token and the previous target word at each step and emits the French translation Ils, regardent, period, and an end-of-sequence marker one token at a time.",
    "caption": "A sequence-to-sequence model has two parts: an encoder that compresses the whole input sequence into a state, and a decoder that, starting from that state and a beginning-of-sequence token, generates the output one token at a time until it emits the end-of-sequence marker. This lets the input and output have different lengths.",
    "credit": "Zhang, Aston and Lipton, Zachary C. and Li, Mu and Smola, Alexander J.",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://d2l.ai/chapter_recurrent-modern/seq2seq.html",
    "provider": "Dive into Deep Learning"
  }
}
