{
  "id": "score-based-model",
  "label": "Score-Based Model",
  "kind": "technique",
  "category": "Generative Models",
  "aliases": [
    "score-based generative model",
    "score matching",
    "NCSN"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A generative model that learns the score, the gradient of the log probability density, of data corrupted at many noise levels, then generates by following that gradient from noise toward where the data is dense. It is the theoretical backbone of diffusion: arriving from a different starting point, it turns out to learn the same object, and its continuous-time SDE view is the language much of modern diffusion theory is written in.",
  "body": [
    {
      "type": "p",
      "text": "A score-based model generates data by learning a vector field that points toward where the data is dense. The score is a precise quantity, the gradient of the log probability density with respect to the input, a vector at every point in data space indicating the direction of steepest increase in likelihood. If you know the score everywhere, you can generate samples by starting from random noise and repeatedly nudging in the score's direction while injecting a little randomness, a procedure called Langevin dynamics that walks samples uphill into the high-density regions where realistic data lives."
    },
    {
      "type": "p",
      "text": "Score-based models matter because they provide the theoretical backbone that explains why diffusion models work, having arrived at the same place from a different starting point. The clean way to estimate a score is to learn it not on the raw data but on data corrupted with noise at many levels, from barely perturbed down to nearly pure noise, and generation then anneals from high noise to low, following the score at each level, which is exactly the trajectory a diffusion model traces in reverse. Recognizing that the noise-prediction target of denoising diffusion and the score-estimation target of these models are equivalent up to a scaling was the unifying insight that connected two research lines into one family."
    },
    {
      "type": "p",
      "text": "Estimating the score directly seems to require knowing the very density you are trying to model, but score matching sidesteps this. Denoising score matching, the practical variant, trains a network to remove noise from corrupted samples, and the optimal denoiser turns out to be a simple rescaling of the score, so the model never needs the true density, only pairs of clean and noised data that are trivial to produce. This is the same simulation-free, regression-style training that makes denoising diffusion scalable, viewed through the lens of densities and gradients rather than noise."
    },
    {
      "type": "p",
      "text": "The continuous-time formulation ties everything together. Casting the noising process as a stochastic differential equation gives a reverse-time equation, driven by the score, that turns noise back into data, and this single view contains diffusion sampling as a special case. It also borders the deterministic transport picture of flow matching, which learns a velocity field rather than a score but pursues the same noise-to-data mapping, and it stands in contrast to a normalizing flow, which models the density exactly through an invertible map instead of estimating only its gradient. The score-based perspective is, in short, the language in which much of the modern theory of diffusion-style generation is written."
    }
  ],
  "related": [
    {
      "slug": "diffusion-model",
      "label": "Diffusion Model",
      "kind": "technique"
    },
    {
      "slug": "denoising-diffusion",
      "label": "Denoising Diffusion",
      "kind": "technique"
    },
    {
      "slug": "flow-matching",
      "label": "Flow Matching",
      "kind": "technique"
    },
    {
      "slug": "normalizing-flow",
      "label": "Normalizing Flow",
      "kind": "technique"
    },
    {
      "slug": "latent-diffusion",
      "label": "Latent Diffusion",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/score-based-model.jpg",
    "alt": "A schematic with two arrows over a strip of images of a dog that grows progressively noisier from left to right. The top arrow, Forward SDE (data to noise), carries the equation dx = f(x,t)dt + g(t)dw. The bottom arrow, Reverse SDE (noise to data), carries the same drift minus a term containing the score, the gradient of log p_t(x), which is boxed and labeled score function.",
    "caption": "A score-based model views generation as a stochastic differential equation. A forward process slowly turns data into noise, and the matching reverse process turns noise back into data. The reverse process needs only the score, the gradient of the log probability density (boxed), which a network learns at every noise level.",
    "credit": "Yang Song (Score-Based Generative Modeling through Stochastic Differential Equations)",
    "license": "Apache-2.0",
    "license_url": "https://github.com/yang-song/score_sde/blob/main/LICENSE",
    "source": "https://github.com/yang-song/score_sde",
    "provider": "GitHub: yang-song/score_sde"
  }
}
