{
  "id": "cross-encoder",
  "label": "Cross-Encoder",
  "kind": "technique",
  "category": "RAG, Embeddings, and Retrieval",
  "aliases": [
    "cross encoder",
    "joint encoder"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A relevance model that takes a query and a candidate document together as a single input and outputs one relevance score, far more accurate than comparing separate embeddings but too slow to scan a whole corpus, so it is used to rerank a shortlist.",
  "body": [
    {
      "type": "p",
      "text": "A cross-encoder scores how well a document answers a query by feeding both into the model at once, joined as a single sequence, and letting full attention run across every query token and every document token before producing one relevance number. This is the most accurate way to judge relevance, because the model can directly compare the two texts term by term rather than through a lossy summary."
    },
    {
      "type": "p",
      "text": "The contrast is with the bi-encoder, the design behind dense retrieval. A bi-encoder embeds the query and each document separately into single vectors and scores by their similarity. That separation is what makes it fast and scalable: document vectors can be computed once, indexed, and searched with approximate nearest neighbor over millions of items. But encoding each text in isolation, with no chance for the query and document to interact, sacrifices accuracy. The cross-encoder makes the opposite trade: it never encodes anything in isolation, so it is precise, but it must run the full model for every query-document pair and cannot precompute or index anything, which makes scanning a large corpus with it hopeless."
    },
    {
      "type": "p",
      "text": "The two are therefore used together in the standard two-stage pipeline. A fast bi-encoder retrieves a shortlist of candidates, the top K by vector similarity, and then a cross-encoder reranks just those K to pick the best N. This is exactly what a reranker is, and it captures most of the cross-encoder's accuracy at a cost bounded by K rather than the corpus size."
    },
    {
      "type": "p",
      "text": "Late interaction sits between the two extremes, keeping per-token vectors to recover much of the cross-encoder's precision while remaining indexable, which is why it is sometimes chosen when reranking every query with a full cross-encoder is too slow."
    }
  ],
  "related": [
    {
      "slug": "reranking",
      "label": "Reranking",
      "kind": "technique"
    },
    {
      "slug": "dense-retrieval",
      "label": "Dense Retrieval",
      "kind": "technique"
    },
    {
      "slug": "embedding-model",
      "label": "Embedding Model",
      "kind": "technique"
    },
    {
      "slug": "late-interaction",
      "label": "Late Interaction",
      "kind": "technique"
    },
    {
      "slug": "semantic-search",
      "label": "Semantic Search",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": []
}
