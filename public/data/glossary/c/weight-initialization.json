{
  "id": "weight-initialization",
  "label": "Weight Initialization",
  "kind": "technique",
  "category": "Deep Learning Architectures",
  "aliases": [
    "weight initialization",
    "Xavier initialization",
    "He initialization"
  ],
  "durable": true,
  "source": "authored",
  "definition": "How a network's weights are set before training begins, a choice that matters far more than it looks. Too small and the signal vanishes through the layers; too large and it explodes; either way a deep network fails to train. Principled schemes, Xavier for symmetric activations and He for ReLU, set the random scale to preserve the signal's magnitude from layer to layer.",
  "body": [
    {
      "type": "p",
      "text": "Before a network can learn, its weights have to start somewhere, and that starting point matters far more than it might seem. The weights are set randomly, but the scale of that randomness is critical. If the initial weights are too small, the signal shrinks as it passes through each layer until activations and the gradients flowing back are effectively zero in the deeper layers, and learning stalls. If they are too large, the signal grows and explodes. Either way a deep network fails to train, not because the architecture is wrong but because it was initialized badly."
    },
    {
      "type": "p",
      "text": "The fix is to choose the initial variance so that the magnitude of activations, and of gradients flowing back, is roughly preserved from layer to layer, neither amplified nor damped as it propagates. Xavier, or Glorot, initialization does this for symmetric activations like tanh by scaling the variance to the number of inputs and outputs of a layer; He initialization adapts the scale for ReLU, which zeroes half its inputs and so needs a larger variance to compensate. These principled schemes replaced naive uniform initialization and were one of the practical unlocks that made training deep networks reliable."
    },
    {
      "type": "p",
      "text": "The reason the scale is so delicate is multiplicative. A signal passing through a deep stack is multiplied by each layer's weights in turn, so if each layer shrinks it even slightly, the product decays geometrically with depth, and if each layer grows it, the product explodes, the same compounding that drives the vanishing gradient and exploding gradient problems. Initialization is the attempt to set every factor in that product near one, so the signal neither dies nor blows up before training has a chance to begin."
    },
    {
      "type": "p",
      "text": "Good initialization works alongside the other stabilizers of deep training. Normalization layers and residual connections also keep signals well-behaved across depth, and together they are why very deep networks train at all. The division of labor is clean: initialization gets training off to a healthy start, and the others keep it healthy as the weights move."
    }
  ],
  "related": [
    {
      "slug": "activation-function",
      "label": "Activation Function",
      "kind": "technique"
    },
    {
      "slug": "vanishing-gradient",
      "label": "Vanishing Gradient",
      "kind": "technique"
    },
    {
      "slug": "exploding-gradient",
      "label": "Exploding Gradient",
      "kind": "technique"
    },
    {
      "slug": "batch-normalization",
      "label": "Batch Normalization",
      "kind": "technique"
    },
    {
      "slug": "relu",
      "label": "Rectified Linear Unit",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": []
}
