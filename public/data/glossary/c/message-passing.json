{
  "id": "message-passing",
  "label": "Message Passing",
  "kind": "technique",
  "category": "Graph and Geometric Learning",
  "aliases": [
    "message-passing neural network",
    "MPNN",
    "neighborhood aggregation"
  ],
  "durable": true,
  "source": "authored",
  "definition": "The core computation of a graph neural network, in which each node computes its new representation by gathering and aggregating messages from its neighbors, then updating itself, repeated layer by layer.",
  "body": [
    {
      "type": "p",
      "text": "Message passing is the unifying recipe behind almost every graph neural network. In one round, each node sends a message along each of its edges, every node aggregates the messages arriving from its neighbors with a permutation-invariant operator, and each node updates its own state by combining the aggregated message with its previous state. Three pieces define a layer: a message function that decides what to send, an aggregation function that combines what arrives, and an update function that produces the new node vector. Most published architectures are just particular choices of these three."
    },
    {
      "type": "p",
      "text": "This pattern matters because it gives the field a single abstraction that captures a sprawling zoo of models. Graph convolution, the graph attention network, and many bespoke architectures all reduce to message passing with different message and aggregation choices. Naming the common skeleton lets researchers reason about expressive power and limitations in general rather than model by model, and it gives engineers one efficient primitive to optimize, the sparse gather-scatter operation, that accelerates the entire family."
    },
    {
      "type": "p",
      "text": "The aggregation step must be invariant to the ordering of a node's neighbors, because a graph carries no inherent order over the nodes adjacent to a given vertex. Sum, mean, and max are the standard choices, and the choice is consequential: a sum can count neighbors and preserves the most information, a mean normalizes away degree, and a max captures the single most salient signal. This permutation invariance is the symmetry that defines the whole approach and the reason a message-passing network is the natural model for data with no canonical layout."
    },
    {
      "type": "p",
      "text": "After k rounds of message passing, a node's representation has absorbed information from everything within k hops, producing a node embedding that encodes both the node's own features and the structure of its surrounding neighborhood. Reading out all node states with another permutation-invariant pool yields a representation of the whole graph. Two practical failure modes shape design. Stacking too many rounds causes over-smoothing, where every node's vector converges to the same value and distinctions wash out; and over-squashing, where information from an exponentially growing neighborhood is crushed through a fixed-width vector across a graph bottleneck."
    },
    {
      "type": "p",
      "text": "Message passing connects directly to the transformer. Self-attention is message passing on the complete graph: every token is a node, every pair of tokens is an edge, the message is the value vector, and the attention score is a learned, content-dependent edge weight. Viewing attention this way, through the lens of geometric deep learning, explains why transformers need positional encoding (a complete graph has no built-in geometry to tell positions apart) and why graph attention networks and transformers are close relatives rather than unrelated inventions."
    }
  ],
  "related": [
    {
      "slug": "graph-neural-network",
      "label": "Graph Neural Network",
      "kind": "technique"
    },
    {
      "slug": "graph-convolution",
      "label": "Graph Convolution",
      "kind": "technique"
    },
    {
      "slug": "graph-attention-network",
      "label": "Graph Attention Network",
      "kind": "technique"
    },
    {
      "slug": "node-embedding",
      "label": "Node Embedding",
      "kind": "technique"
    },
    {
      "slug": "self-attention",
      "label": "Self-Attention",
      "kind": "technique"
    },
    {
      "slug": "transformer",
      "label": "Transformer",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/message-passing.png",
    "alt": "A central node x0 connected to four neighbor nodes x1, x2, x3, x4; each neighbor's feature passes through a message function psi, and the four messages flow inward to update x0.",
    "caption": "One round of message passing: the central node x0 gathers a message from each neighbor, every message computed by the same function psi, then aggregates them with a permutation-invariant operator to update its own state. Stacking k such rounds lets a node's representation absorb everything within k hops.",
    "credit": "NickDiCicco",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://commons.wikimedia.org/wiki/File:Message_Passing_Neural_Network.png",
    "provider": "Wikimedia Commons"
  }
}
