{
  "id": "convolutional-neural-network",
  "label": "Convolutional Neural Network",
  "kind": "technique",
  "category": "Deep Learning Architectures",
  "aliases": [
    "CNN",
    "ConvNet",
    "convolutional network"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A neural network for grid data like images that slides small learnable filters across the input, reusing the same weights at every position. That weight sharing is not a trick but a baked-in assumption, that a pattern means the same thing wherever it appears, which lets a CNN see images with a tiny fraction of the parameters a fully connected network would need.",
  "body": [
    {
      "type": "p",
      "text": "A convolutional neural network is best understood as a fully connected network forbidden from wasting its parameters. A multilayer perceptron connects every input pixel to every neuron, which for an image means millions of weights and no notion that a pixel's neighbors matter more than a pixel clear across the frame. A CNN throws most of those connections away on purpose. It slides a small filter across the image, computing a weighted sum over a little local patch at each position, and crucially it reuses the same filter weights at every position. What looks like a brand-new architecture is really two hard assumptions about images turned into structure."
    },
    {
      "type": "p",
      "text": "Those two assumptions are locality and translation invariance, and they are the whole reason CNNs work. Locality says that meaningful patterns, an edge, a corner, a texture, are local, detectable from a small neighborhood, so a neuron has no business staring at the entire image at once. Translation invariance says a pattern means the same thing wherever it lands, so the filter that finds a vertical edge in the top left should be the identical filter that finds it in the bottom right. Sharing one filter across all positions enforces exactly that, and as a side effect it collapses the parameter count from millions to a few hundred per filter. A CNN is an MLP with most of its weights forced to be either zero (locality) or equal to one another (sharing), and that constraint is not a limitation, it is the prior knowledge that makes learning from images tractable at all."
    },
    {
      "type": "p",
      "text": "Mechanically a CNN interleaves three kinds of layer. Convolutional layers apply their filters to produce feature maps recording where each learned pattern appears. A nonlinear activation function, usually relu, follows each one. Pooling layers downsample, summarizing a region into a single value, which shrinks the maps and grants a little tolerance to small shifts. Stacked, these build a hierarchy: early layers fire on edges and color blobs, middle layers on parts, deep layers on whole objects, the same composition-of-features story depth always tells, here grounded in space. The stack usually ends in a few fully connected layers that read the final features into class scores, and the whole network trains end to end by backpropagation."
    },
    {
      "type": "p",
      "text": "Pushing CNNs deeper exposed the problems that drove a decade of fixes: the vanishing gradient that stalled very deep stacks until the residual connection gave gradients a clean path, and the unstable activations that batch normalization tamed. With those in hand, convolutional networks ran computer vision for most of the 2010s. The challenge since has come from the opposite design philosophy. Where a CNN bakes the spatial priors in by hand, the vision transformer throws them out and lets attention learn relationships between image patches from data, trading the CNN's built-in efficiency for raw flexibility at scale. That contest is a clean instance of the field's deepest recurring tension, hand-designed structure against learned generality, the same tension the bitter lesson keeps adjudicating."
    }
  ],
  "related": [
    {
      "slug": "multilayer-perceptron",
      "label": "Multilayer Perceptron",
      "kind": "technique"
    },
    {
      "slug": "activation-function",
      "label": "Activation Function",
      "kind": "technique"
    },
    {
      "slug": "relu",
      "label": "Rectified Linear Unit",
      "kind": "technique"
    },
    {
      "slug": "residual-connection",
      "label": "Residual Connection",
      "kind": "technique"
    },
    {
      "slug": "batch-normalization",
      "label": "Batch Normalization",
      "kind": "technique"
    },
    {
      "slug": "backpropagation",
      "label": "Backpropagation",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/convolutional-neural-network.svg",
    "alt": "Data flow through the LeNet convolutional network: a 28 by 28 input image feeds a convolution to a 6-channel 28 by 28 feature map, a pooling step to a 6-channel 14 by 14 map, a second convolution to a 16-channel 10 by 10 map, a second pooling to a 16-channel 5 by 5 map, and then three fully connected (dense) layers ending in 10 outputs.",
    "caption": "A convolutional network interleaves convolution layers, which slide shared filters over the grid to produce feature maps, with pooling layers that downsample. Early layers detect simple local patterns and deeper layers compose them; the stack ends in fully connected layers that map the learned features to class scores. This is LeNet, the architecture that established the pattern.",
    "credit": "Zhang, Aston and Lipton, Zachary C. and Li, Mu and Smola, Alexander J.",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://d2l.ai/chapter_convolutional-neural-networks/lenet.html",
    "provider": "Dive into Deep Learning"
  }
}
