{
  "id": "gaussian-process",
  "label": "Gaussian Process",
  "kind": "technique",
  "category": "Probabilistic Machine Learning",
  "aliases": [
    "GP",
    "Gaussian processes"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A probability distribution not over numbers but over entire functions, where any finite set of function values is jointly Gaussian. It is the cleanest way to do regression with honest, calibrated uncertainty, confidence that widens far from the data and narrows near it, and its one real limitation is a cubic cost that caps it at a few thousand points.",
  "body": [
    {
      "type": "p",
      "text": "A Gaussian process is a probability distribution not over numbers or vectors but over entire functions. The defining property is simple: for any finite collection of input points, the corresponding function values follow a joint multivariate Gaussian distribution. A Gaussian process is fully specified by a mean function, saying where the functions are centered, and a covariance function or kernel, saying how strongly the values at two inputs are correlated. Because it places a prior directly on functions, it is a nonparametric model: its flexibility grows with the data instead of being fixed by a chosen number of weights."
    },
    {
      "type": "p",
      "text": "Gaussian processes matter because they are one of the cleanest ways to do regression with honest uncertainty. Most models return a single best guess; a Gaussian process returns a full predictive distribution at every input, so it reports not just the expected value but how confident it is, and that confidence widens far from the observed data and narrows near it, exactly the behavior you want when decisions depend on knowing what the model does not know. This makes them a default tool in Bayesian optimization, where each expensive experiment is chosen by trading predicted value against predicted uncertainty, and in active learning, geostatistics, and the modeling of small scientific datasets."
    },
    {
      "type": "p",
      "text": "The mechanics rest on conditioning a joint Gaussian on the observed outputs. Given training inputs and their noisy targets, the kernel builds a covariance matrix encoding the assumed smoothness of the function, and the standard Gaussian conditioning formulas produce a closed-form posterior mean and variance at any new input. The kernel is where the modeling choices live: a squared-exponential kernel assumes very smooth functions, others encode periodicity or abrupt changes, and the kernel's free parameters, length scale and signal variance, are typically tuned by maximizing the marginal likelihood, an instance of maximum likelihood estimation applied to the model evidence rather than to point predictions."
    },
    {
      "type": "p",
      "text": "The chief limitation is cost. Exact inference inverts an n-by-n covariance matrix, which scales cubically in the number of training points and makes naive Gaussian processes impractical beyond a few thousand observations. The field's response is a family of sparse and approximate methods built on a small set of inducing points, frequently fit with variational inference, which turns the intractable posterior into a tractable optimization. Conceptually, a Gaussian process is the infinite-dimensional cousin of Bayesian inference over a finite parameter vector: a Bayesian linear model with an infinite, cleverly chosen feature expansion is exactly a Gaussian process under the hood, the limit you reach when you stop committing to a fixed parametric form and instead reason directly about the space of functions that could have produced your data."
    }
  ],
  "related": [
    {
      "slug": "gaussian-distribution",
      "label": "Gaussian Distribution",
      "kind": "technique"
    },
    {
      "slug": "bayesian-inference",
      "label": "Bayesian Inference",
      "kind": "technique"
    },
    {
      "slug": "probability-distribution",
      "label": "Probability Distribution",
      "kind": "technique"
    },
    {
      "slug": "maximum-likelihood-estimation",
      "label": "Maximum Likelihood Estimation",
      "kind": "technique"
    },
    {
      "slug": "latent-variable-model",
      "label": "Latent Variable Model",
      "kind": "technique"
    },
    {
      "slug": "variational-inference",
      "label": "Variational Inference",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/gaussian-process.png",
    "alt": "A plot of Gaussian process regression on a noise-free dataset. A dotted line shows the true function f(x) equals x sin(x), six blue dots mark the observations, an orange line is the mean prediction, and an orange shaded band is the 95 percent confidence interval. The band pinches to almost nothing at each observation and flares wide in the gaps between points, especially on the far left where there is no data.",
    "caption": "A Gaussian process returns not a single guess but a full predictive distribution at every input. The mean prediction (orange line) passes through the observations, and the shaded 95 percent confidence interval collapses to near zero at the data points and widens in the gaps, dramatically so on the left where no points constrain it. That widening band is the model reporting how much it does not know, which is the property that makes Gaussian processes a default for Bayesian optimization and active learning.",
    "credit": "scikit-learn developers",
    "license": "BSD-3-Clause",
    "license_url": "https://github.com/scikit-learn/scikit-learn/blob/main/COPYING",
    "source": "https://scikit-learn.org/stable/auto_examples/gaussian_process/plot_gpr_noisy_targets.html",
    "provider": "scikit-learn"
  }
}
