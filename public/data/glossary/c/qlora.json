{
  "id": "qlora",
  "label": "QLoRA",
  "kind": "technique",
  "category": "Training and Fine-Tuning",
  "aliases": [
    "quantized low-rank adaptation",
    "quantized LoRA"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A memory-efficient method that freezes a large model quantized to 4-bit and trains small low-rank LoRA adapters on top, letting a single consumer GPU fine-tune models that would otherwise need a cluster. It works because the frozen base never updates (so it can stay compressed) and is dequantized to higher precision on the fly for each operation, so storage is 4-bit but the math is not.",
  "body": [
    {
      "type": "p",
      "text": "QLoRA combines two ideas to make fine-tuning large models affordable: quantize the frozen base model down to 4 bits so it barely fits in memory, then train only small low-rank adapters on top of it, the LoRA recipe. The base weights are never updated, so they can stay in their compressed form throughout training, and all the learning happens in the lightweight adapters, whose gradients and optimizer state are tiny. The result is that a model which would normally require many high-end GPUs to fine-tune can be adapted on a single consumer card."
    },
    {
      "type": "p",
      "text": "Making 4-bit training work without wrecking quality took a few specific innovations, and the central one is the keeper. The 4-bit NormalFloat data type is information-theoretically suited to the roughly normal distribution of neural network weights; double quantization compresses the quantization constants themselves for further savings; and paged optimizers use the GPU's unified memory to absorb the memory spikes that would otherwise cause out-of-memory errors during training. Crucially, computation happens by dequantizing weights to a higher precision on the fly for each operation, so the math is done at adequate precision even though storage is 4-bit."
    },
    {
      "type": "p",
      "text": "The impact was practical and large: QLoRA put fine-tuning of capable open models within reach of individuals and small teams, and it became a default for instruction-tuning and domain adaptation on limited hardware. Quality is typically very close to full-precision LoRA and often close to full fine-tuning, with the gap depending on the task."
    },
    {
      "type": "p",
      "text": "Like LoRA, it inherits the deployment convenience of adapters: the small trained weights can be stored, shared, and swapped cheaply, and merged back into the base model if a single set of weights is preferred at serving time."
    }
  ],
  "related": [
    {
      "slug": "lora",
      "label": "LoRA",
      "kind": "technique"
    },
    {
      "slug": "parameter-efficient-fine-tuning",
      "label": "Parameter-Efficient Fine-Tuning",
      "kind": "technique"
    },
    {
      "slug": "quantization",
      "label": "Quantization",
      "kind": "technique"
    },
    {
      "slug": "fine-tuning",
      "label": "Fine-Tuning",
      "kind": "technique"
    },
    {
      "slug": "mixed-precision-training",
      "label": "Mixed Precision Training",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/qlora.png",
    "alt": "Three columns comparing finetuning methods by where memory sits and how gradients flow. Full finetuning has a 32-bit optimizer state and gradients flowing into a 16-bit base model. LoRA freezes a 16-bit base model and trains small 16-bit adapters. QLoRA freezes a 4-bit base model, trains 16-bit adapters, and adds a paging arrow that offloads optimizer state to CPU memory.",
    "caption": "QLoRA makes fine-tuning a large model fit on one GPU by stacking two ideas. The base model is quantized to 4 bits and frozen (so its weights never update and can stay compressed), small LoRA adapters carry all the learning, and paged optimizers spill optimizer state to CPU memory to absorb spikes. Weights are dequantized to higher precision on the fly for each operation, so the math stays accurate while storage is 4-bit.",
    "credit": "Tim Dettmers, Artidoro Pagnoni, Ari Holtzman, Luke Zettlemoyer",
    "license": "CC BY 4.0",
    "license_url": "https://creativecommons.org/licenses/by/4.0",
    "source": "https://arxiv.org/abs/2305.14314",
    "provider": "arXiv"
  }
}
