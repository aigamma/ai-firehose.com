{
  "id": "node-embedding",
  "label": "Node Embedding",
  "kind": "technique",
  "category": "Graph and Geometric Learning",
  "aliases": [
    "node embeddings",
    "graph embedding"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A learned vector representation of a node in a graph, placing each node at a coordinate in a continuous space so that graph-proximate or functionally similar nodes lie close together.",
  "body": [
    {
      "type": "p",
      "text": "A node embedding is a fixed-length vector that represents a node of a graph, the graph counterpart of a word or token embedding. The goal is to map the discrete, relational structure of a graph into a continuous vector space where geometric closeness reflects graph closeness: nodes that are neighbors, that share many neighbors, or that play similar structural roles should land near one another. Once nodes live in such a space, standard machine learning tools that expect vectors, classifiers, clustering, nearest-neighbor search, can be applied to graph data directly."
    },
    {
      "type": "p",
      "text": "Node embeddings matter because they are the bridge between irregular graph structure and the rest of the machine learning toolkit. With good embeddings you can predict a node's label, score whether two nodes should be linked by measuring the distance between their vectors, recommend items, or retrieve similar entities, all as ordinary vector operations. The same vectors serve many downstream tasks, which is why learning them well is often the central objective when working with graphs."
    },
    {
      "type": "p",
      "text": "There are two broad ways to produce them. Shallow methods, such as random-walk approaches that adapt the skip-gram idea from word embeddings, learn one independent vector per node by treating short walks over the graph as sentences and pushing co-visited nodes together. Modern methods instead use a graph neural network: through several rounds of message passing or graph convolution, each node aggregates information from its neighborhood, and its final hidden state is its embedding. The neural approach has a decisive advantage, it is inductive, computing embeddings as a function of node features and local structure, so it can embed nodes and even entire graphs never seen during training, whereas shallow methods must relearn a vector for every new node."
    },
    {
      "type": "p",
      "text": "Node embeddings connect to the broader geometry of representation learning. The space they inhabit is a learned latent space, and the manifold hypothesis applies: meaningful nodes tend to concentrate on a low-dimensional surface within the embedding space, so that distance along the data manifold, not just raw coordinates, carries the semantics. In this sense an embedding model for graphs is doing the same job as one for text or images, turning discrete objects into geometry where similarity becomes distance, with the difference that the structure being encoded is the connectivity of the graph itself."
    }
  ],
  "related": [
    {
      "slug": "graph-neural-network",
      "label": "Graph Neural Network",
      "kind": "technique"
    },
    {
      "slug": "message-passing",
      "label": "Message Passing",
      "kind": "technique"
    },
    {
      "slug": "graph-convolution",
      "label": "Graph Convolution",
      "kind": "technique"
    },
    {
      "slug": "embedding-model",
      "label": "Embedding Model",
      "kind": "technique"
    },
    {
      "slug": "latent-space",
      "label": "Latent Space",
      "kind": "technique"
    },
    {
      "slug": "manifold-hypothesis",
      "label": "Manifold Hypothesis",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/node-embedding.png",
    "alt": "Left, a graph with a biased random walk highlighted; right, the same nodes placed in a two-dimensional embedding space where they form two clusters, one red and one blue, with a single bridge node between them.",
    "caption": "A node embedding maps each node of a graph to a coordinate in a continuous space so that graph-proximate nodes land close together. Here biased random walks over the graph (left) are turned into vectors (right), where the two communities separate into distinct clusters and the bridging node sits between them; closeness in the space now means closeness in the graph.",
    "credit": "Carlos Eduardo Ravello Joo",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://commons.wikimedia.org/wiki/File:Grover-leskovec-2016-node2vec.svg",
    "provider": "Wikimedia Commons"
  }
}
