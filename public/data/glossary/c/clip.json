{
  "id": "clip",
  "label": "CLIP",
  "kind": "technique",
  "category": "Computer Vision",
  "aliases": [
    "Contrastive Language-Image Pretraining"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A model trained to align images and text in a shared embedding space by contrastive learning over large numbers of image-caption pairs, enabling zero-shot classification and image-text retrieval.",
  "body": [
    {
      "type": "p",
      "text": "CLIP, short for Contrastive Language-Image Pretraining, is a model that learns a single shared embedding space for both images and text. It is built from two encoders, an image encoder (commonly a [vision transformer](vision-transformer)) and a text encoder (a transformer over tokens), trained jointly so that an image and its caption land near each other in that space while unrelated image-text pairs land far apart. Released by OpenAI in 2021 and trained on roughly four hundred million image-caption pairs scraped from the web, CLIP became the default bridge between the visual and textual worlds."
    },
    {
      "type": "p",
      "text": "CLIP matters because it turns recognition into a matching problem against natural language, which unlocks zero-shot classification. To classify an image into arbitrary categories you never trained on, you simply embed the candidate label names as text (for example \"a photo of a dog\", \"a photo of a cat\"), embed the image, and pick the label whose embedding is closest. There is no fixed label set and no [fine-tuning](fine-tuning) required, a sharp departure from conventional [image classification](image-classification), which is locked to the categories it was trained on. The same shared space powers text-to-image [semantic search](semantic-search), image deduplication, and the conditioning signal inside text-to-image generators."
    },
    {
      "type": "p",
      "text": "The training objective is [contrastive learning](contrastive-learning). Within each large batch of image-text pairs, the model computes the similarity between every image embedding and every text embedding, forming a grid of scores. It is trained so the matching diagonal pairs score highest and all the off-diagonal mismatches score low, using a symmetric cross-entropy loss over the rows and columns. This pushes the two encoders to agree on a common geometry of meaning, so that proximity in the [latent space](latent-space) reflects semantic similarity across both modalities. The result is a general-purpose [embedding model](embedding-model) for vision grounded in language."
    },
    {
      "type": "p",
      "text": "CLIP sits at the foundation of modern multimodal systems. Its frozen image encoder is a popular visual backbone, its text-image alignment is the conditioning mechanism behind diffusion-based image generators, and its embeddings feed retrieval pipelines. By learning from naturally occurring captions rather than hand-curated labels, it inherits the breadth of the open web, which is the source of both its remarkable generality and its biases."
    }
  ],
  "related": [
    {
      "slug": "contrastive-learning",
      "label": "Contrastive Learning"
    },
    {
      "slug": "vision-transformer",
      "label": "Vision Transformer"
    },
    {
      "slug": "embedding-model",
      "label": "Embedding Model"
    },
    {
      "slug": "semantic-search",
      "label": "Semantic Search"
    },
    {
      "slug": "image-classification",
      "label": "Image Classification"
    },
    {
      "slug": "latent-space",
      "label": "Latent Space"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": []
}
