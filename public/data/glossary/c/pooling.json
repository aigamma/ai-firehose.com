{
  "id": "pooling",
  "label": "Pooling",
  "kind": "technique",
  "category": "Computer Vision",
  "aliases": [
    "max pooling",
    "average pooling"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A downsampling operation in convolutional networks that summarizes each small neighborhood of a feature map into a single value, shrinking spatial resolution while retaining the strongest or average response.",
  "body": [
    {
      "type": "p",
      "text": "Pooling is a downsampling operation that reduces the spatial size of a feature map by summarizing each small region into one number. A pooling layer slides a window, often two by two, across the feature map and replaces the values inside each window with a single summary: max pooling keeps the largest value, average pooling keeps the mean. Striding the window by its own size halves the height and width, so a stack of pooling layers progressively shrinks a large image down to a small, dense grid of features. It is one of the two basic building blocks, alongside convolution, of the classic [convolutional neural network](convolutional-neural-network)."
    },
    {
      "type": "p",
      "text": "Pooling matters for two reasons. First, it cheaply reduces resolution, which cuts the amount of computation and memory the deeper layers must handle, letting the network afford more channels and depth. Second, it grants a degree of translation invariance: because max pooling reports only that a strong feature appeared somewhere in the window, not exactly where, small shifts of the input leave the output unchanged. This makes the learned representation robust to the object being a few pixels left or right, which is desirable for [image classification](image-classification), where only the presence of an object matters, not its precise position."
    },
    {
      "type": "p",
      "text": "In practice, max pooling dominated early convolutional networks because keeping the strongest activation tends to preserve the most salient evidence of a feature. A special case, global average pooling, collapses an entire feature map to a single value per channel and is the standard way to turn a spatial feature grid into a fixed-length vector for the final classifier, replacing the parameter-heavy fully connected layers that older networks used. Pooling also defines the levels of a [feature pyramid](feature-pyramid), since each successive pooled map represents the image at a coarser scale."
    },
    {
      "type": "p",
      "text": "The same invariance that helps classification hurts tasks that need precise location, so pooling is used carefully elsewhere. [Semantic segmentation](semantic-segmentation) and [object detection](object-detection) must recover spatial detail that pooling discards, which is why segmentation networks pair an aggressively pooled encoder with an upsampling decoder and skip connections. Many modern architectures reduce their reliance on pooling, achieving downsampling instead through strided convolutions, and the [vision transformer](vision-transformer) abandons it altogether in favor of patching and attention."
    }
  ],
  "related": [
    {
      "slug": "convolutional-neural-network",
      "label": "Convolutional Neural Network",
      "kind": "technique"
    },
    {
      "slug": "image-classification",
      "label": "Image Classification",
      "kind": "technique"
    },
    {
      "slug": "semantic-segmentation",
      "label": "Semantic Segmentation",
      "kind": "technique"
    },
    {
      "slug": "feature-pyramid",
      "label": "Feature Pyramid",
      "kind": "technique"
    },
    {
      "slug": "object-detection",
      "label": "Object Detection",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/pooling.png",
    "alt": "A 4 by 4 grid of numbers divided into four colored 2 by 2 windows, with an arrow to a 2 by 2 output holding the maximum of each window: 6, 8, 3, 4.",
    "caption": "Max pooling downsamples a feature map by sliding a window and keeping only the largest value in each region. Each colored 2 by 2 block collapses to its maximum (6, 8, 3, 4), halving width and height and granting a small amount of translation invariance because only the strongest response is kept, not its exact position.",
    "credit": "Aphex34",
    "license": "CC BY-SA 4.0",
    "license_url": "https://creativecommons.org/licenses/by-sa/4.0",
    "source": "https://commons.wikimedia.org/wiki/File:Max_pooling.png",
    "provider": "Wikimedia Commons"
  }
}
