{
  "id": "fully-sharded-data-parallel",
  "label": "Fully Sharded Data Parallel",
  "kind": "technique",
  "category": "Systems and Infrastructure",
  "aliases": [
    "FSDP",
    "ZeRO",
    "sharded data parallelism"
  ],
  "durable": true,
  "source": "authored",
  "definition": "A distributed training strategy that shards a model's parameters, gradients, and optimizer states across data-parallel workers and gathers each layer's weights only when needed, so models far larger than one device's memory can be trained without slicing the model itself.",
  "body": [
    {
      "type": "p",
      "text": "Fully sharded data parallel is a way to train models that do not fit on a single accelerator while keeping the simplicity of data parallelism. Plain data parallelism replicates the entire model on every worker and has each process a different slice of the batch. That is easy but wasteful: the parameters, the gradients, and the optimizer states (which for an optimizer like Adam are larger than the parameters themselves) are duplicated on every device, so the largest trainable model is capped by one device's memory."
    },
    {
      "type": "p",
      "text": "Fully sharded data parallel removes the duplication by sharding all three of those tensors across the workers, so each holds only its slice. The key move is doing this just in time: right before a layer runs, the workers all-gather its full parameters from their shards, compute the forward or backward pass, and then immediately discard the gathered copy, keeping only their shard. Gradients are reduce-scattered so each worker ends up with the averaged gradient for its slice. Peak memory is therefore set by the largest single layer that must be materialized, not by the whole model, which is what lets a cluster train models orders of magnitude larger than any one device could hold. This is the same idea introduced as ZeRO, whose stages progressively shard optimizer states, then gradients, then parameters."
    },
    {
      "type": "p",
      "text": "The cost is communication. Gathering weights and scattering gradients every layer moves a lot of data across the interconnect, so the approach depends on fast links between devices and on overlapping that communication with computation to hide its latency. It composes with the other forms of parallelism: tensor and pipeline parallelism split an individual model across devices for the cases where even one layer is too large, and fully sharded data parallel handles scaling across the replicas."
    },
    {
      "type": "p",
      "text": "It also pairs with the standard memory savers, mixed-precision training and gradient checkpointing, which reduce the size of what must be stored and gathered. Together these are the toolkit that makes training models with hundreds of billions of parameters feasible."
    }
  ],
  "related": [
    {
      "slug": "data-parallelism",
      "label": "Data Parallelism",
      "kind": "technique"
    },
    {
      "slug": "model-parallelism",
      "label": "Model Parallelism",
      "kind": "technique"
    },
    {
      "slug": "tensor-parallelism",
      "label": "Tensor Parallelism",
      "kind": "technique"
    },
    {
      "slug": "pipeline-parallelism",
      "label": "Pipeline Parallelism",
      "kind": "technique"
    },
    {
      "slug": "mixed-precision-training",
      "label": "Mixed Precision Training",
      "kind": "technique"
    },
    {
      "slug": "gradient-checkpointing",
      "label": "Gradient Checkpointing",
      "kind": "technique"
    }
  ],
  "attention": 0,
  "first_seen": "2026-06-02",
  "rotation": null,
  "neighbors": [],
  "top_items": [],
  "axis_positions": [],
  "image": {
    "src": "/images/glossary/fully-sharded-data-parallel.png",
    "alt": "A diagram decomposing an all-reduce into a reduce-scatter followed by an all-gather across four GPUs. On the left, all-reduce leaves every GPU with the full summed result. In the middle, reduce-scatter leaves each GPU with only its own reduced slice. On the right, all-gather rebuilds the full set of slices on every GPU.",
    "caption": "Fully sharded data parallel shards the parameters, gradients, and optimizer state so each GPU stores only a slice. As shown, the all-reduce of standard data parallelism is split into a reduce-scatter (each device keeps just its averaged slice) and an all-gather (the full weights are reassembled only when a layer is about to run, then discarded). Peak memory is set by the largest single layer, not the whole model.",
    "credit": "PyTorch Contributors",
    "license": "BSD-3-Clause",
    "license_url": "https://github.com/pytorch/tutorials/blob/main/LICENSE",
    "source": "https://docs.pytorch.org/tutorials/intermediate/FSDP_tutorial.html",
    "provider": "PyTorch"
  }
}
