---
title: Frontier Model
slug: frontier-model
kind: opinion
category: Transformers and LLMs
aliases: frontier model, frontier AI
related: foundation-model, large-language-model, scaling-laws, emergent-ability, reasoning-model
summary: A term for the most capable general-purpose AI models at the cutting edge of the field, the handful of large foundation models whose abilities define the current state of the art and draw both excitement and safety-policy scrutiny.
---

A frontier model is one of the few most capable general-purpose models at any given moment, the ones pushing the boundary of what AI can do. It is a deliberately moving target: today's frontier models are surpassed within months, so the term names a position at the leading edge rather than any fixed system. In practice it points to the largest, newest, most expensive-to-train models from the small set of labs that can build them, the ones topping benchmarks and human-preference rankings.

The phrase carries weight beyond marketing because of safety and policy. "Frontier AI" has become the label for models whose novel and not-yet-understood capabilities, and the risks that come with them, warrant special scrutiny, from voluntary safety commitments to proposed regulation. Calling a model a frontier model is partly a claim about capability and partly a claim that it sits in the zone where new risks first appear.

What distinguishes a frontier model is the combination the field's trends predict: the scale that scaling laws reward, the emergent abilities that appear only at that scale, and increasingly the inference-time reasoning that the newest systems add.

This site is, in a sense, an instrument for watching the frontier: its whole job is to surface what is new and what is breaking out across the AI field, which is largely the story of frontier models and the techniques that define them.
