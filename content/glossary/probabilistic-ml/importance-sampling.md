---
title: Importance Sampling
slug: importance-sampling
kind: technique
category: Probabilistic Machine Learning
aliases: importance sampling estimator
related: markov-chain-monte-carlo, expectation, probability-distribution, variational-inference, evidence-lower-bound, kl-divergence
summary: A Monte Carlo trick for estimating an expectation under a distribution you cannot sample, by drawing from an easier proposal distribution and reweighting each sample by how much the target favors it. It is unbiased no matter the proposal, but it lives or dies by that choice: a poor match makes a few samples dominate and the estimate useless.
---

Importance sampling estimates an expectation under a distribution you cannot easily sample from by sampling the wrong distribution on purpose and then correcting. You draw from a different, more convenient distribution, the proposal, and weight each sample by the ratio of the target density to the proposal density at that point, the importance weight. Averaging the quantity of interest times its weight gives an unbiased estimate of the expectation under the target, even though not a single sample came from the target itself.

The situation it addresses is pervasive: you can write down a target distribution and evaluate its density, but you have no direct way to draw from it, which is the everyday predicament of Bayesian computation, where the target is an intractable posterior. It is also the natural tool for estimating rare-event probabilities, where naive sampling almost never lands in the region of interest, so the proposal is deliberately shifted to oversample the rare region and the weights restore correctness. The same reweighting logic recurs throughout machine learning whenever samples are generated under one distribution but an expectation is needed under another, as in off-policy reinforcement learning.

The mechanics are simple but the practice is delicate, and the practice is where it lives or dies. Multiply each sample's value by its weight, average, and you have your estimate; a self-normalized variant divides by the sum of the weights, removing the need to know the target's normalizing constant at the cost of a small bias. But the method lives or dies by the proposal: if it is a poor match for the target, especially if it has lighter tails so the target places mass where the proposal rarely visits, a handful of samples acquire enormous weights and dominate the average, making the estimate high-variance and unreliable. The effective sample size, which collapses when a few weights dominate, is the standard diagnostic for this failure.

Importance sampling sits alongside Markov chain Monte Carlo as one of the two foundational Monte Carlo strategies, and the two are complementary: importance sampling draws independent samples and parallelizes trivially but degrades badly in high dimensions, whereas MCMC explores high-dimensional targets adaptively but produces correlated samples. It also threads into variational methods, where an importance-weighted version of the evidence lower bound uses several weighted samples to squeeze the bound closer to the true log evidence, improving the training of latent variable models like the variational autoencoder. In that role importance sampling is less a standalone algorithm than a reweighting principle that sharpens the estimators at the core of probabilistic machine learning.
