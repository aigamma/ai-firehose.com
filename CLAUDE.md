# AI Firehose, Codebase Guide

Project context: ai-firehose.com is a personal AI-industry intelligence dashboard, structurally an outlier hunt. It ingests the most salient signal across the AI field every day, organizes it with an embedding and RAG substrate, and surfaces what is new and what is breaking out across four nested time depths: Day, Week, Month, and Quarter. It is the third in a personal trilogy. aigamma.com was built to learn PhD-level math by interacting with models directly. worldthought.com was built to learn philosophy and the interconnections between major thinkers. AI Firehose is built so its author can feel organized and courageous facing the daily flood of AI developments, and stay on the bleeding edge. It is personal first; if others find it useful, good. Keep that north star in every design choice: the job is to turn an overwhelming firehose into something navigable and conquerable, not to build one more noisy feed.

This document is auto-loaded into every agent's context. If a contributor (human or AI) reads only one file, this is it. It is the canonical source of conventions. `AGENTS.md` delegates here.

## Branching and Pull Requests Are Forbidden. Commit Linearly to `main`, Always.

**This is a solo, single-author project with exactly ONE line of history: `main`. NEVER create a branch. NEVER open a pull request. NEVER push to any ref other than `main`. There are no exceptions short of Eric explicitly instructing otherwise in the moment.**

Every commit lands directly on `main` and is pushed to `origin`. If a session begins on any other branch, switch to `main` before you commit (`git checkout main`). Do not create a feature branch "to be safe", do not open a PR "for review", and do not use any worktree or agent isolation that creates a non-`main` branch. There is no reviewer, no merge step, and no collaborator to coordinate with. A branch or a PR here buys nothing and costs real damage: it fragments history into parallel diverging lines and strands finished work off the trunk, where it is invisible and rots.

This rule is written with deliberate, earned emphasis. The branch-per-session habit repeatedly fractured this repo into parallel branches (video-keypoints, site-polish, observability) behind three open pull requests, and left a shipped feature (the transcript-grounded video insights) missing from `main` while this very document claimed it was live. A feature that is not on `main` is not done, no matter what a branch holds. Linear history on `main` is non-negotiable.

## Writing Rules (apply to all generative output)

Em dashes (the long dash, U+2014) are forbidden in any generative text on this project: UI copy, JSX strings, code comments, Markdown docs, JSON artifact content (summaries, definitions, cluster names, headlines), and all prompts shipped to models. Replace an em dash with the punctuation the sentence actually wants: comma, colon, semicolon, period, parentheses, or "and". En dashes (U+2013) are allowed only for numeric ranges. Model-generated prose (classifier summaries and stances, glossary definitions) is additionally sanitized through `worker/lib/text.mjs` before it enters the corpus, because a prompt instruction alone does not guarantee a model complies. Verbatim source titles are quotes, not generative text, and are left intact.

Headings and titles use Title Case. Cite claims: AI-written summaries and definitions must trace to the item they describe.

Do not use "RRG" or "Relative Rotation Graph": those name a trademark. This project uses the older prior-art math, Mansfield Relative Performance (Roy Mansfield, 1979), and the neutral terms "rotation ratio" and "rotation momentum", mirroring aigamma.com/rotations. Cite Mansfield where the methodology is explained.

## Pacing Constraints

Eric is on Claude Max 20x with consistent unused weekly headroom and pre-paid overage credits. Do not throttle token usage, insert sleep gaps, or split independent work across wakeups to conserve cache. Optimize for wall-clock time and visible progress.

Do not use `/loop` for backlog work. For "burn through N known independent items," spawn N parallel `Agent` subagents in a single message; wall-clock is the slowest single subagent, not the sum. Use `/schedule` (cron in Anthropic's cloud) for genuine multi-hour persistence that must survive session close. Do not end a turn early just to schedule the next wakeup; when more work is in front of you, keep doing it in the same turn.

Do not ask for confirmation between batches of independent work. Run to completion. External rate limits (OpenAI, Fly.io, GitHub, Netlify, YouTube) still apply; this rule is about Anthropic-side conservatism only.

Commit liberally and verbosely: the dev machine takes frequent hardware faults, so a commit is the floor you fall back to. Push on a genuine completion or stall, promptly, and do not leave finished work unpushed for long. With frequent faults and the occasional clean re-image, the GitHub remote is the only durable store, so a completed-but-unpushed increment is one fault away from gone. Avoid a push per tiny commit (each push triggers a Netlify build), but err toward pushing a completion soon rather than batching for hours. Leaving finished work committed but unpushed is the failure that causes the deployed tree and local tree to silently diverge.

## Documentation as Durable Source of Truth, and the Dialectical Absorption Protocol

This is the cultural keystone of the project, and the reason the agent harness does not go stale.

Insights, rules, decisions, and process improvements that future contributors need MUST land in this repository's documentation, not in session memory or chat history. Session memory decays; the repo does not. The hierarchy is `CLAUDE.md` (auto-loaded) then `STEERING_DOCS.md` (the map) then the tier-2 subsystem docs in `docs/` then `LESSONS_LEARNED.md` (append-only cumulative wisdom).

The protocol every session follows:

1. Before acting, read `LESSONS_LEARNED.md` and the tier-2 doc for the subsystem you are about to touch.
2. When you discover a process improvement, a sourcing rule, a failure mode, or a better contract, commit that insight to the right doc before the turn ends. The lesson from one session must be absorbed into the wisdom for the next, with enduring notes that retain value cumulatively.
3. When a working rule conflicts with the docs, fix the docs (or fix the rule and make the alignment explicit in the commit). A working tree where the docs lie is a process failure, no different from code that lies.
4. Every commit that materially changes a subsystem updates that subsystem's tier-2 doc in the same commit.
5. `docs/INGESTION_LOG.md` records each pipeline run (what, when, by which agent or model, counts, anomalies). It is the substrate for tuning the rotation windows and thresholds.

### Sources of Truth and How to Update Them

Private session memory and chat transcripts decay across sessions; the repository does not. So when you learn something durable, route it to the right home, do not leave it in a session note. A working tree where the docs lie is a process failure, no different from code that lies.

| What you learned or changed | Where it goes |
|---|---|
| A project-wide convention, contract, or north-star decision | `CLAUDE.md` (this file): the relevant section, plus the `Current State` section when a subsystem's shape changes |
| A subsystem detail (pipeline, embedding, sources, ops, deploy) | that subsystem's tier-2 doc in `docs/`, in the same commit as the code |
| A lesson, failure mode, or "do this not that" from a session | `LESSONS_LEARNED.md` (append-only, newest on top) |
| A per-run record (counts, anomalies, what ran) | `docs/INGESTION_LOG.md` (append-only) |
| A change to the document hierarchy itself, or a new tier-2 doc | `STEERING_DOCS.md` (the map) |
| A repeatable way to onboard a feature | `docs/FEATURE_PLAYBOOK.md` |
| A structural constant (kind, horizon, quadrant, axis, threshold, retention) | `src/data/registry.js`, the single source of truth the code derives from |
| An AI concept, tool, or technique that deserves a durable glossary entry but is missing | Append it to `docs/glossary_queue.md` (the agent-curated candidate queue) after dedup-checking it is not already a durable entry, a prospect in `docs/glossary_backlog.md`, or already queued. A future expansion pass authors from the queue. |

Any agent, in any tool (Claude Code, Cursor, Copilot, OpenCode, Antigravity Gemini, Codex, Aider), inherits this rule through the thin pointer files that delegate here. The pointers never duplicate content, so there is one source of truth and no drift.

This section applies to itself: it lives here, in the auto-loaded doc, because the rule about durable docs is itself a durable insight.

### Enforcement: the Anti-Staleness Gates

The durable-docs rule is enforced by code, not goodwill, so a working tree where the docs, the knowledge graph, generated artifacts, or the writing rules lie fails CI instead of drifting silently. 5 committed checker scripts guard against drift. Four are wrapped in `node:test` so `npm test` runs them; the generated-artifact gate is a standalone CI step because it rewrites JSON before diffing it (`check_docs_fresh` judges the COMMITTED tree via `git ls-files`, never the local disk, so local and CI agree):

- `scripts/check_docs_fresh.mjs`: every backtick-quoted source path and `npm run` script named in the current-state docs (`CLAUDE.md`, `AGENTS.md`, `STEERING_DOCS.md`, `README.md`, the cross-vendor pointers, the tier-2 `docs/`) must exist. The append-only history logs (`LESSONS_LEARNED.md`, `docs/INGESTION_LOG.md`) are excluded by design, since they legitimately name files later deleted. A `scripts/docs_stale_baseline.json` ratchet tracks any pre-existing debt so the gate fails only on NEW drift; do not add entries to silence a fresh break, fix the doc.
- `scripts/check_glossary.mjs`: three knowledge-graph integrity checks. Every authored `related:` slug (which `build_glossary.mjs` silently drops when it does not resolve) AND every step of every curated learning path (`public/data/learning-paths.json`) must resolve to a real concept hub; and no authored alias is ambiguous (`findAmbiguousAliases`), that is, claimed by more than one concept with no label owner, since the wiki-linker (`richtext.js`) would then auto-link that term to an arbitrary concept, a silent mislink.
- `scripts/check_no_emdash.mjs`: no em dash (U+2014) appears in authored generative text (the glossary content, `src/` UI copy, JSX, and CSS, the docs, the shipped `worker/pipeline/prompts/`, and authored JSON), enforcing the project's first writing rule. Verbatim source titles in the corpus artifacts are exempt and not scanned; model output is sanitized separately at ingest by `worker/lib/text.mjs`.
- `scripts/check_doc_accuracy.mjs`: public count claims and model-tier prose must match the generated glossary, atlas, checker inventory, and `MODELS` registry, so exact numbers in `README.md`, `OVERVIEW.md`, `docs/GLOSSARY.md`, and this file do not silently go stale.
- `scripts/check_generated_fresh.mjs`: runs the deterministic glossary, harness, and creators generators, then fails on a diff in their served artifacts. This is run by CI via `npm run check:generated`, not inside `node --test`, so tests never race a JSON rewrite.

How to apply: when you add a class of cross-reference that could rot, extend or copy these checks rather than trusting review; `git add` new files before running a gate, since it judges the staged or committed tree; and treat a one-shot gate failure as possibly a race with a concurrent commit, re-checking before you "fix" it.

## Architecture

Subsystems, each in its own directory:

- `src/` React 18 + Vite frontend. Reads precomputed JSON from `/data` (served from `public/data/`). The registry at `src/data/registry.js` is the single source of truth: kinds, horizons, quadrants, concept axes, retention, taxonomy thresholds, and navigation all derive from it. Dark-first theme with a real light toggle.
- `worker/` The Fly.io ingestion worker (pure Node; no Python source lives in the repo, though the worker container also bundles `yt-dlp`, `ffmpeg`, and the `python3` runtime the `yt-dlp` binary requires, for the transcript path). Source adapters under `worker/sources/` (YouTube is primary). The pipeline under `worker/pipeline/` runs fetch, dedupe, transcript, classify, embed, upsert, rotation, network, prune, then commits artifacts. Model-facing prompts live in `worker/pipeline/prompts/`.
- The embedding substrate lives in the worker, not in a separate top-level directory: `worker/lib/voyage.mjs` (Voyage embeddings and rerank), `worker/lib/pinecone.mjs` (vector upsert and query), and `worker/lib/retrieve.mjs` (two-stage retrieval), with the network precompute (centroids, clusters, spectrums, neighbors, influence) in `worker/pipeline/network.mjs`. Reimplemented natively from the civil approach, all JavaScript.
- `netlify/functions/` One light read function, `retrieve.mjs`, the non-chat semantic search proxy (Pinecone plus Voyage rerank). No chatbot.
- `public/data/` The committed JSON artifacts the site reads: `clusters.json`, `spectrums.json` (positions-only; axis vectors held in the worker cache), `influence.json`, `stats.json`, `creators.json` and `directory.json` (the Watch surface: the latest-videos spotlight and the browse educator directory), plus `attention/`, `digests/` (per-horizon `<horizon>.json` plus the agentic `briefing_<horizon>.json`), and `glossary/` (a slim `index.json` for the list and search plus one `c/<slug>.json` hub per concept, fetched on demand). A served artifact ships only what a page renders.
- `docs/` Tier-2 subsystem docs. `sources/` Curated source registries (for example `youtube_channels.json`). `content/glossary/` The durable, authored knowledge layer: one Markdown file per concept, compiled into the served glossary by `scripts/build_glossary.mjs`.
- `mcp/` A standalone, publishable MCP server (`ai-firehose-mcp`, `mcp/index.mjs`) that wraps the public read API so any MCP client (Claude Code, Claude Desktop) can query the site over stdio: four tools (`search_ai`, `whats_new`, `define`, `stats`). It needs no repo checkout, no keys, and no local data (it calls `ai-firehose.com`), depends only on the MCP SDK, and ships just `mcp/index.mjs` plus `mcp/README.md`. Independent of the site build and the worker; see `mcp/README.md`.

Heavy and long-running work lives on the Fly worker because it exceeds Netlify function limits. Netlify serves the static site and the one read function.

## Core Contracts

These are non-negotiable. They are what let a fresh agent ingest new information idempotently and without guessing.

### Idempotency

Content hash every chunk (`sha256(text).slice(0,16)`). Vector IDs are deterministic (`<kind>::<source>::<slug>::<hash16>` for corpus, `glossary::<slug>` for durable entries). `worker/.cache/vector_manifest.json` records text and metadata hashes for each vector the worker owns, so unchanged content is skipped, metadata-only drift updates without Voyage, and orphaned vectors are deleted from a manifest diff rather than a routine full Pinecone listing. Re-running the pipeline on unchanged data is a no-op. Ported from `C:\civil\rag\ingest.mjs` and `D:\worldthought.com\scripts\rag\ingest.mjs`, adapted to this repo's committed clone-fresh worker state.

### Retention: A Self-Expiring Corpus

Nothing older than one quarter is kept. A prune stage (idempotent, keyed on `published_at`) deletes vectors from Pinecone and items from the artifacts once they pass `RETENTION_DAYS` (default 100, in `src/data/registry.js`). The four horizons are nested windows inside this single rolling quarter. The clusters and spectrums therefore always depict the current frontier, and Pinecone and Voyage costs stay flat no matter how long the site runs: each run embeds only new or changed text hashes and reserves full Pinecone listing for manual drift audits. Glossary concepts are the one durable layer: a concept hub persists as lightweight reference, but with no items inside the window it is marked dormant and drops off the live boards. On top of this sits the deliberate permanent layer: a durable, Opus-authored knowledge base under `content/glossary` (foundational, advanced, and exotic concepts marked `durable: true`), never pruned, merged into the served glossary by `scripts/build_glossary.mjs`. See `docs/GLOSSARY.md`.

### Classification Without Guessing

Each item is classified by Claude against a strict JSON schema (structured output). The model returns `kind` with a one-line justification, a factual `summary`, `concepts[]`, `entities[]`, and `stance` for opinions. `kind` is assigned by the classifier, not by the source, because one YouTube video can teach a technique, demo a tool, and argue an opinion. Never invent a mapping. Today this is a single strict-schema Claude pass; the optional OpenAI dual-check that would route disagreements to staging is intended but not yet implemented. The discipline is deterministic-or-flagged.

### The AI-Grown Taxonomy (Concept Resolution)

Tags and concepts are not a fixed vocabulary. The classifier discovers candidate concepts in each item, and the model coins a canonical name plus a cited definition for genuinely new ones. Every candidate is then fitted loosely to the existing taxonomy by embedding similarity (voyage-3 cosine), so near-duplicates collapse onto one concept with aliases instead of fragmenting. Thresholds live in `registry.js` `TAXONOMY`, the single source of truth, injected into the resolver: bind when cosine is at or above `mergeThreshold` (0.86); in the band down to `reviewFloor` (0.78) bind only when a lexical signal agrees (a shared significant token, or one label is an acronym of the other), which keeps distinct short "AI X" labels apart; below `reviewFloor` the model coins a new canonical concept. New concepts currently enter the taxonomy automatically once resolved; the `_proposed.json` human-approval staging gate is intended but not yet implemented in `worker/pipeline/concepts.mjs`. The full algorithm is in `docs/INGESTION.md`. Resolution currently runs only against the corpus taxonomy, so a discovered tag does not yet collapse onto a durable glossary concept (the worker fix is tracked in `docs/ROADMAP.md`); until then `scripts/fold_corpus_concepts.mjs` folds duplicate corpus tags onto their durable hub on the committed snapshot (the pure, unit-tested matcher is `scripts/lib/fold.mjs`), so clicking a trending tag like "AI agents" lands on the rich authored `ai-agent` hub instead of a thin statistics page. See `docs/GLOSSARY.md`.

### Determinism in Precompute

Pinned PCA start vector, deterministic k-means init (seed-spread, no RNG), stable sort, content-hash gating. The one-command `network` rebuild regenerates every dependent artifact in dependency order and is safe to re-run. Ported from civil's stage-14 `network` step.

### No Chatbot

The embedding layer powers organization, visualization, and a live semantic search surface only. There is no conversational agent. robotlogic.org proved this non-chat embedding machinery; that is exactly what is being cloned.

## Stack and Keys

Pinecone (vector store, index `ai-firehose`, voyage-3 at 1024-dim cosine, single namespace plus metadata filters). Voyage (`voyage-3` embeddings, `rerank-2` for live search). Claude, in three tiers chosen by stakes (`MODELS` in `worker/lib/anthropic.mjs`): `bulk` (Haiku) classifies each item, `quality` (Sonnet) is the middle tier kept for cluster naming and future passes, and `enduring` (Opus 4.8) writes the durable, high-stakes prose a smart reader keeps, the daily briefing (`briefing.mjs`) and the glossary definitions (`define.mjs`). The rule: permanent, low-volume content gets the strongest model, day-to-day churn gets the cheap one. LLM cluster naming and an OpenAI dual-check are intended but not yet wired. OpenAI also hosts the Whisper API (`whisper-1`) used by the optional YouTube transcript fallback (`worker/pipeline/transcript.mjs`, gated by `ENABLE_TRANSCRIPTS`). Keys are pulled from sibling repo `.env` files at build time, then set as Fly.io secrets and Netlify environment variables. Never commit keys.

**Spend discipline (these keys are metered money).** The Anthropic API, OpenAI, Voyage, and Pinecone keys all bill real money and are reserved for the DEPLOYED production runtime (the scheduled worker and cron). Do NOT run code that calls a paid external API from an interactive session, `npm run ingest`, any generate/embed/define/briefing/transcribe/image script, or a backfill loop, without explicit permission. A Claude Code agent's own model usage (its reasoning and subagents) is billed to the user's Max subscription, not the API, so a one-time backfill driven in a session is done ON the subscription (the main thread or subagents author the output themselves) rather than by calling `worker/lib/anthropic.mjs`. See `docs/OPERATIONS.md` and `LESSONS_LEARNED.md` Session 29.

## Multi-Agent Collaboration

`AGENTS.md` is a hub-and-spoke delegator that points here, so cross-vendor agents (Codex, Cursor Composer, Antigravity Gemini, Aider) discover these conventions through the canonical doc. The ensemble can build concurrently against the deterministic, idempotent contracts above. Suggested division of labor once the scaffold exists: Claude builds the embedding and precompute layer, the rotation math, and the dashboard; Codex builds the source adapters and the from-scratch YouTube plus Whisper worker; Cursor Composer ports the visualization components and theming; Antigravity Gemini builds the Netlify function and deploy plumbing.

The cross-project prompt library is at `D:\prompts` (per-model directories plus a `private` tree). Operational prompts the worker ships live in `worker/pipeline/prompts/` in this repo. Proprietary or experimental prompt engineering lives under `D:\prompts\private\ai-firehose.com`, which is local-only. Hard rule from `D:\prompts\CLAUDE.md`: everything under `private/` is gitignored and must never be published or force-added.

## Documentation Map

See `STEERING_DOCS.md` for the tiered map and a "when to read what" cheat sheet. Tier-2 docs: `docs/INGESTION.md` (the pipeline, concept resolution, retention), `docs/RAG.md` (embedding substrate, Pinecone and Voyage, precompute artifacts and schemas, the citation contract), `docs/SOURCES.md` (every source adapter, YouTube primary, the featured-creators curation workflow), `docs/OPERATIONS.md` (keys, schedules, costs, recovery), `docs/DEPLOYMENT.md` (Fly and Netlify), `docs/GLOSSARY.md` (the durable knowledge layer: authoring, the build, the durability contract, and wiki-style auto-linking), `docs/GLOSSARY_ONBOARDING.md` (the active campaign onboarding the discovered backlog: procedure and live status), `docs/FEATURE_PLAYBOOK.md` (the repeatable recipe for onboarding a feature, with a dedicated fully-RAG-integrated path). Cumulative wisdom: `LESSONS_LEARNED.md`. Run log: `docs/INGESTION_LOG.md`. Forward backlog: `docs/ROADMAP.md`. Cross-vendor agent delegation: `AGENTS.md` plus the thin pointers `.cursor/rules/project-context.mdc`, `.github/copilot-instructions.md`, `opencode.md`, `GEMINI.md`.

## Commands

- `npm install` then `npm run dev` runs the site locally.
- `npm run build` produces the static site in `dist/`.
- `npm run check:generated` rebuilds generated read-surface artifacts and fails if they differ from the working tree.
- `npm run ingest` runs one pipeline pass on the worker (Phase 1+).
- The deterministic Phase 2+ rebuild runs as the network stage of `npm run ingest`; `node worker/pipeline/recompute_boards.mjs` replays the committed store to rebuild the attention boards offline.

## Current State

**Glossary durable layer: complete and fully illustrated.** The durable knowledge layer holds 644 Opus-authored entries across 33 categories, and as of 2026-06-09 every standing campaign has closed. The onboarding backlog (corpus-discovered concepts that lacked an authored entry) is cleared to zero, recorded in `docs/GLOSSARY_ONBOARDING.md`. The full quality re-pass (Phase C, status and per-category ledger in `docs/GLOSSARY_QUALITY_PASS.md`) re-read all 644 entries end-to-end against the earned-vividness bar in `content/glossary/README.md`, made only 7 opener reworks and embedded 2 figures, and left the rest byte-identical, establishing that the layer was already roughly 99% at-bar. The prose passes ran linear and main-thread at full Opus strength (no subagents for drafting; a subagent may run the adversarial opening-sentence audit, which is verification). The figures forward item is now closed too: all 644 entries carry a cited illustrative figure. The final 209 gaps (abstract or arXiv-locked concepts with no clean open-license image) were filled by an AI-generation pass, each figure generated by OpenAI `gpt-image-1` via `scripts/glossary_generate_images.mjs` under the plain-labeling policy, reviewed individually on the main thread before shipping (an AI generator gets diagrams subtly wrong, and a wrong teaching figure is worse than none), then validated by the committed `glossary_images.mjs finalize` oracle; `docs/glossary_image_wishlist.md` is now empty. The campaigns and their work queues are mapped in `STEERING_DOCS.md`.

The site is feature-complete and deployed. Build and the worker test suite (`npm test`) are green, and CI (`.github/workflows/ci.yml`) runs `npm ci`, `npm run check:generated`, `npm test`, and `npm run build` on every push and PR to `main`. Routes are code-split (Home eager, the rest lazy behind a Suspense boundary). The GitHub repo is public and MIT-licensed at `https://github.com/aigamma/ai-firehose.com` and `main` is pushed; the Netlify site `ai-firehose` is live with continuous deploy from `main` and a verified `/api/retrieve`; DNS is switched to Netlify and propagating with the cert issued. The scheduled data refresh is live: `data: scheduled ingestion` commits now land on `main` (the GitHub Actions cron running the worker pipeline), so the corpus updates automatically each cycle. A dedicated Fly worker deploy may still follow, but the refresh no longer depends on it.

**Pipeline (`worker/`).** Seven source adapters behind one aggregator (`worker/sources/index.mjs`): YouTube (primary, with a captions-then-Whisper transcript path gated by `ENABLE_TRANSCRIPTS`), Hacker News, arXiv, GitHub, blogs and newsletters (`sources/blogs.json`), Hugging Face daily papers, and Reddit (403 from datacenter IPs; works from residential or Fly). `run.mjs` runs: fetch, classify (Claude), the AI-grown concept resolution (`concepts.mjs`), manifest-gated embed and upsert (Voyage, Pinecone, `worker/.cache/vector_manifest.json`), the per-board heat signal each board ranks by (`delta` and `trend`, the growth in attention this window versus the prior equal window, `windowTrend` in `attention.mjs`) plus the Mansfield rotation values (`rotation.mjs`) that now feed the concept hubs; board construction is the shared pure `boards.mjs`, replayed offline by `recompute_boards.mjs` (which anchors its windows to the newest item in the store for a deterministic replay), so the board-building logic is single-sourced), the network precompute (`network.mjs`: centroids, clusters, spectrums, neighbors, influence), AI-written glossary definitions (`define.mjs`) then the durable authored-knowledge merge (`scripts/build_glossary.mjs`, so a run never drops the permanent layer) and the durable-glossary sync into Pinecone (`embed_glossary.mjs`, `glossary::` vectors, stored `text` metadata, and hash gating, so semantic search is aware of the knowledge layer without re-embedding all durable entries daily), digests, the agentic daily briefing (`briefing.mjs`, an Opus 4.8 synthesis cached on a hash of the window state, sanitized and cited) and the agentic Watch-cycle digest (`watch_digest.mjs`, the same Opus-synthesis-cached-on-a-hash pattern over the latest educator videos, weighting the recommended inner circle), the per-video pages (`videos.mjs`: the nearest videos by cosine similarity, plus transcript-grounded study notes (a write-up, 3 to 6 key points, and 4 to 8 timestamped chapters, prompt in `worker/pipeline/prompts/video_insights.mjs`) authored from YouTube captions, served under `public/data/videos/`; `videos.mjs` PRESERVES the insights across a rebuild and a metadata-only write-up is the fallback where captions are absent. The library was backfilled on the Max subscription, one subagent per video writing to a staging cache then merged deterministically by `scripts/merge_video_insights.mjs`, captions fetched gently by `scripts/batch_fetch_transcripts.mjs`; see `docs/SOURCES.md`), manifest-driven vector deletion, sitemap, RSS, stats, and the featured-creators resolver (`scripts/build_creators.mjs`) plus the creators-directory resolver (`scripts/build_directory.mjs`, pure core in `scripts/lib/directory.mjs`), both corpus-only and deterministic (live RSS in the creators spotlight is an explicit `--live` opt-in the build never uses), both of which also run as a `prebuild` step on every Netlify deploy. An accumulating retention-pruned store (`worker/.cache/items.json`), the vector manifest, and a classification cache keep runs stable and cheap. YouTube Shorts are excluded end to end (they read as a trick, taking longer to open than to watch): the adapter drops them at ingest via the cached `/shorts/<id>` redirect oracle (`worker/sources/youtube_shorts.mjs`, committed verdicts in `worker/.cache/shorts.json`, fail-open and bounded by `SHORTS_PROBE_MAX`), `run.mjs` guards the store as a backstop so one chokepoint keeps every downstream artifact short-free, and `scripts/remove_shorts.mjs` is the one-time purge (it removed the 130 Shorts that predated the filter from the corpus, Pinecone, and the Watch and digest surfaces). Corpus is roughly 930 items. The worker also emits LLM cost and token telemetry to a central OpenTelemetry collector (`worker/lib/otel.mjs`, a zero-dependency OTLP-over-fetch emitter, fail-open and env-gated, dormant until `OTEL_EXPORTER_OTLP_ENDPOINT` is set); see `docs/OBSERVABILITY.md`.

**Site (`src/`, Netlify).** Home is an editorial front page, not a dashboard: it opens directly on a hairline dateline (horizon switch with arrow-key nav, the window phrase, the freshness stamp) sitting just under the global header, with the page `h1` (the `SITE` nameplate) kept but visually hidden (`.sr-only`) so a redundant on-page masthead spends no real estate above the fold (the brand already lives in the header; the old promotional tagline and standfirst were removed earlier), leading with the agentic daily briefing (`Briefing.jsx`, a severity-tinted, cited prose lede from `digests/briefing_<horizon>.json`) as the lead story, then a Breaking Out callout, the What Moved trend boards (`TrendBoard.jsx`, one per kind: the topics that gained or lost the most attention this window versus the prior equal window, with a magnitude bar, a signed `delta` and arrow, a shaded color-coded row for breakouts, and a just-surfaced badge), a Watch teaser, and a Fresh Off the Wire grid of cited `ItemCard`s. The whole page rises in on a staggered load (respecting `prefers-reduced-motion`). The per-kind deep views render the same `TrendBoard` as a full ranked list. The Watch page (`/watch`) is video-first: it opens with the agentic Watch-cycle digest (`digests/watch.json`, `src/components/WatchDigest.jsx`, an Opus 4.8 paraphrase of the most salient developments across the latest videos this cycle, weighting the recommended inner circle, hash-gated and cited like the daily briefing so it refreshes only when new videos land), then a Latest grid of recent video tiles (`public/data/videos/index.json`, `src/components/VideoCard.jsx`: thumbnail plus a one-line summary, substance over tags; no YouTube Shorts, which are excluded from the corpus at ingest and guarded again in `selectVideos`) that open a dedicated on-site page `/watch/:videoId` (`src/pages/VideoPage.jsx`) with the embedded talk (`LiteYouTube`), transcript-grounded study notes (a write-up, a key-points list, and timestamped chapters that SEEK the embedded player on-site via a `startAt`/`playToken` control rather than bouncing to YouTube), the concepts it covers, and a Similar rail of the nearest videos by cosine similarity (per-video payloads `public/data/videos/<id>.json`, built by `worker/pipeline/videos.mjs`). The channel roster demotes to a secondary Browse by Educator strip (`directory.json`, `src/components/CreatorDirectory.jsx`) listing every ingested educator, with a carefully vetted inner circle of `recommended` channels badged with a gold star labeled Most Focused and sorted to the front (a purely editorial, hand-set tier from `youtube_registry.mjs recommend`, never touching the rotation math; the internal field is `recommended`, the badge reads Most Focused); nothing is featured in the stream past the three-month retention window (and hand-pinned videos self-expire from the record 90 days after pinning, `scripts/prune_pins.mjs`), in parity with the corpus prune, and a per-channel `hide_from_directory` flag (set by onboarding `--track-only`, cleared by `--endorse`) ingests and tracks a channel for signal without listing it, since the directory is an endorsement surface. A fresh list of handles is onboarded with one command (`scripts/onboard_youtube.mjs`; runbook `docs/ONBOARD_YOUTUBE_CHANNEL.md`). The Glossary leads with a durable, Opus-authored knowledge base (644 entries across 33 categories, from foundations and advanced mathematics, through deep learning, reinforcement learning, and mechanistic interpretability, to cognitive science and the philosophy of mind), grouped by category, with an Atlas view that maps the 33 categories as a navigable, cross-linked constellation (`public/data/glossary/atlas.json`, built deterministically by `scripts/lib/atlas.mjs` and unit-tested), and woven into the live trending taxonomy by wiki-style auto-linking (`RichText.jsx` and `richtext.js`); per-concept hubs (the authored body with wiki-links and a related mesh, plus the corpus momentum quadrant, neighbors, and items where present) fetched on demand, each a centered reading column that leads with a cited, illustrative figure chosen to teach the concept's defining insight (all 644 concepts carry such a figure, self-hosted under `public/images/glossary/`, each credited to its author, license, and source page, with a click-to-enlarge lightbox; deliberately sourced across a WIDE RANGE so the layer does not read as a Wikipedia scrape: roughly 68% from over 70 open providers (about 30% Wikimedia Commons, plus CC-BY arXiv papers, Dive into Deep Learning, scikit-learn, Hugging Face, Distill, Google for Developers, OpenAI Spinning Up, PyTorch, TensorFlow, CC-BY journals (PLOS, eLife, Frontiers), Our World in Data, and many MIT/BSD/Apache repos), and the remaining 32% AI-generated by OpenAI `gpt-image-1` under a plain-labeling policy (carried as the `AI-generated` license token, credited to the model and date) for the abstract or arXiv-locked concepts no open image fit; license-validated by `scripts/glossary_images.mjs`, generated by `scripts/glossary_generate_images.mjs`, rendered by `src/components/GlossaryFigure.jsx`); a spaced-repetition Flashcard Review (`src/pages/Review.jsx`, `/review`) that schedules each durable concept to return as it is about to be forgotten (an SM-2-lite scheduler in the pure, unit-tested `src/lib/srs.js`, persisted in the browser via `src/hooks/useSrs.js` under `aifh:srs:v1`), with a kind filter, keyboard flip, four-key grading, per-card management (see less often, challenging, favorite, remove, in the scheduler and surfaced as a toolbar so a card is never an inescapable nag), and a client-side gamification layer (XP and levels, mastery toward the whole glossary, a per-category mastery map, and an achievements wall, with deliberately no streaks and no daily quota, both rejected as loss-aversion dark patterns) in the pure, unit-tested `src/lib/game.js` via `src/hooks/useGame.js` under `aifh:game:v1`; a Learn page (`src/pages/Learn.jsx`, `/learn`) of curated learning paths through the glossary (`public/data/learning-paths.json`, whose steps the `check_glossary` gate validates), with per-step progress tracked in the browser (`src/hooks/useLearnProgress.js`): a progress bar, tickable steps, and a Resume link; a returning-visitor layer that makes the firehose conquerable, all client-side (`src/lib/seen.js`, `src/hooks/useSeen.js`): a "new since your last visit" signal, per-item read state with a "you have cleared this window's wire" completion banner and a mark-all control, and dateline nudges for cards due in Review; Explore (semantic search via `netlify/functions/retrieve.mjs`, themes, spectrums, connections); the interior reading pages (Methodology with live stats, About, and the living `/harness` demo) are centered editorial columns with a shared `src/components/PageHero.jsx` masthead (`.article` in `src/styles/theme.css`), modeled on worldthought.com room pages; RSS, sitemap, robots, Open Graph meta, favicon; dark-first theme with a light toggle. The global header carries only a few primary nav pills (Home, the three kind boards, and Glossary: `NAV_PRIMARY` in `src/data/registry.js`), with every other destination in a right-anchored dropdown Menu (`src/components/NavMenu.jsx`, `NAV_MORE`) whose panel is opaque so it never collides with the page behind it; the old header search (the command palette) is removed pending a fix for the transparency-collision bug that motivated it. Skip link and `main` landmark. Every concept is a link into its hub.

**Performance.** The site scores a perfect Lighthouse pass: 100 in Performance, Accessibility, Best Practices, and SEO on both mobile and desktop. The landing view is built to render in a single paint. An `inlineHomeData` plugin in `vite.config.js` reads the default-horizon artifacts at build time (the horizon is `DEFAULT_HORIZON` from `src/data/registry.js`: the briefing, the three attention boards, the digest, and a slim first-tiles videos list) and injects them as `window.__HOME_DATA__`, which `src/lib/useData.js` seeds its cache from synchronously, so the first React commit paints the whole above-the-fold view with a real LCP element, no progressive-insertion layout shift (CLS 0), and a tight Speed Index (the artifacts are the same committed worker output `/data` serves, so the inline snapshot never drifts from the deploy). The front-page entrance animation animates transform only, never opacity, since an opacity:0 start hid the LCP element on throttled mobile; and the below-the-fold recent-videos rail carries `content-visibility: auto`, because its off-screen horizontal overflow otherwise suppressed Chrome's LCP candidate selection under mobile emulation (the NO_LCP error). For Accessibility, in-text prose links carry underlines (a non-color cue) and the menu and video-card controls let their accessible name derive from their visible text. `public/llms.txt` serves the agentic-browsing surface as a real static file, where the SPA catch-all previously returned HTML for it. See `LESSONS_LEARNED.md` Session 32.

**Notes.** Local `public/data` holds a real run's output (the demo seed is replaced). Served artifacts ship only what a page renders: the glossary index is slim and each concept hub loads on demand, spectrums are positions-only with the axis vectors held in `worker/.cache`. The one-time migration that reshaped the committed artifacts is `worker/pipeline/slim_artifacts.mjs` (idempotent). The `ai-firehose` Pinecone index lives in the civil project, isolated by name.

**MCP server (`mcp/`).** A standalone `ai-firehose-mcp` package exposes the public read API over the Model Context Protocol (four stdio tools: `search_ai`, `whats_new`, `define`, `stats`), wrapping `ai-firehose.com` with no keys or local data, installed via `npx -y ai-firehose-mcp`. Independent of the site build and the worker.

**Pending**: a dedicated Fly worker deploy (the scheduled data refresh already runs via the GitHub Actions cron, landing `data: scheduled ingestion` commits on `main`), and let DNS finish propagating to Netlify. Minor stretch: an X (Twitter) adapter; deepen the durable glossary entries (more QA passes; all 644 already carry a cited figure).

When a subsystem changes, update this section and the relevant tier-2 doc in the same commit. Keep this section evergreen and scannable, not a chronological pile.
